---
phase: 23-client-360-crosslink-fix
plan: 03
type: execute
wave: 3
depends_on:
  - "23-02"
files_modified:
  - web/src/components/admin/clients/admin-client-requests-tab.tsx
  - web/src/components/admin/clients/admin-client-automations-tab.tsx
autonomous: false
requirements:
  - CLNT-04
  - AUTM-02

must_haves:
  truths:
    - "Clicking 'View all requests' from /admin/clients/[id] lands on /admin/requests filtered to that org (chip visible, rows scoped)"
    - "Clicking 'View all automations' from /admin/clients/[id] lands on /admin/automations filtered to that org (chip visible, rows scoped)"
    - "The slug emitted by both tabs is encodeURIComponent-safe and matches the slug-or-uuid resolver's slug path"
    - "Round-trip through the Client 360 → list page → ✕ chip → back works end-to-end in EN and ES"
  artifacts:
    - path: "web/src/components/admin/clients/admin-client-requests-tab.tsx"
      provides: "Emitter for /admin/requests?org= cross-link — verified to use slug + encodeURIComponent"
    - path: "web/src/components/admin/clients/admin-client-automations-tab.tsx"
      provides: "Emitter for /admin/automations?org= cross-link — verified to use slug + encodeURIComponent"
  key_links:
    - from: "web/src/components/admin/clients/admin-client-requests-tab.tsx"
      to: "/admin/requests?org={slug}"
      via: "Link href"
      pattern: "encodeURIComponent\\(orgSlug\\)"
    - from: "web/src/components/admin/clients/admin-client-automations-tab.tsx"
      to: "/admin/automations?org={slug}"
      via: "Link href"
      pattern: "encodeURIComponent\\(orgSlug\\)"
---

<objective>
Verify the emitters in the Client 360 tabs already match the slug-or-uuid resolver convention chosen in Plan 01, fix any tactical bug found, and human-verify the full round-trip end-to-end.

Purpose: Plan 01 + 02 made the data layer and the list pages handle `?org=<slug>` correctly. This plan closes the loop by confirming the emitters (which the user touches first) are aligned, and by running the user-perspective acceptance test for both success criteria #1 and #2 of the phase. No new files; minimal-or-no code changes expected.

Output:
- Confirmed emitter behavior + any tactical patch if a bug surfaces during inspection
- Manual UAT screenshots/notes via the human-verify checkpoint
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-client-360-crosslink-fix/23-CONTEXT.md
@.planning/phases/23-client-360-crosslink-fix/23-01-PLAN.md
@.planning/phases/23-client-360-crosslink-fix/23-02-PLAN.md
@web/src/components/admin/clients/admin-client-requests-tab.tsx
@web/src/components/admin/clients/admin-client-automations-tab.tsx

<interfaces>
<!-- Current emitter contract (CONFIRMED via Read during planning — no change expected). -->

From admin-client-requests-tab.tsx:135-140:
```tsx
<Link
  href={`/admin/requests?org=${encodeURIComponent(orgSlug)}`}
  className="inline-block text-sm text-purple-700 hover:underline dark:text-purple-300"
>
  {translations.viewAll}
</Link>
```

From admin-client-automations-tab.tsx:139-144:
```tsx
<Link
  href={`/admin/automations?org=${encodeURIComponent(orgSlug)}`}
  className="inline-block text-sm text-purple-700 hover:underline dark:text-purple-300"
>
  {translations.viewAll}
</Link>
```

Both already emit `?org=<encoded-slug>` per CONTEXT.md decision ("Emitter keeps emitting slug with encodeURIComponent"). Expected outcome: zero code changes. If inspection reveals a divergence (e.g., the slug wasn't actually being passed in, or a downstream component re-encodes), apply the minimal fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm emitter behavior + tactical patch if needed</name>
  <files>
    web/src/components/admin/clients/admin-client-requests-tab.tsx
    web/src/components/admin/clients/admin-client-automations-tab.tsx
  </files>
  <action>
    Read both files in their entirety and verify:

    1. **Slug source**: `orgSlug` prop is the organization slug (kebab-case ASCII) passed in from the parent server component (`admin-client-detail.tsx` or the page). Trace the prop back ONE level to confirm it's `organization.slug` and not the UUID. Use Grep:
       ```
       grep -n "orgSlug" web/src/components/admin/clients/admin-client-detail.tsx
       grep -n "AdminClientRequestsTab\|AdminClientAutomationsTab" web/src/app/\(admin\)/admin/clients/\[id\]/page.tsx
       ```

    2. **Encoding**: confirm `encodeURIComponent(orgSlug)` is the only transformation applied — no double-encoding, no toLowerCase, no character substitution.

    3. **Target URLs**: confirm hrefs are exactly `/admin/requests?org=...` and `/admin/automations?org=...` — no locale prefix (next-intl uses cookie-based locale in this project), no trailing slash.

    4. **Edge case**: if the slug ever contains a character that breaks the resolver's lookup (e.g., the org was created with an uppercase slug despite the slug column being lower-case), document it as a known limitation in the SUMMARY. No code change unless we actually find a real broken slug in seed data.

    **If all three checks pass, this task makes ZERO code changes** — it is a verification task. Document findings in the SUMMARY.

    **If a divergence is found** (e.g., the prop was actually a UUID, the encoding was double-applied, the href shape was wrong), apply the minimal fix and commit. Do not refactor; do not extract; do not change the visual styling. The fix is one-liner-grade.

    No console.log, no `any`. Strict TypeScript.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit src/components/admin/clients/admin-client-requests-tab.tsx src/components/admin/clients/admin-client-automations-tab.tsx && grep -n "encodeURIComponent(orgSlug)" src/components/admin/clients/admin-client-requests-tab.tsx src/components/admin/clients/admin-client-automations-tab.tsx</automated>
  </verify>
  <done>
    Both emitters confirmed to emit `/admin/{requests|automations}?org=${encodeURIComponent(orgSlug)}`. `orgSlug` traced back to `organization.slug` (not UUID). Either zero code changes (most likely) or a documented one-line fix landed. `npx tsc --noEmit` passes on both files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end UAT for both success criteria + EN/ES parity</name>
  <what-built>
    Phase 23 ships the `?org=` deep-link fix:
    - Plan 01: slug-or-uuid resolver inside both admin list queries
    - Plan 02: `/admin/requests` and `/admin/automations` parse `?org=` and render a filter chip (active or "not found" variant); EN/ES i18n keys added
    - Plan 03: emitters verified to match the resolver convention
  </what-built>
  <how-to-verify>
    Run `npm run dev` from `web/`. Open `http://localhost:3000` in a clean browser session and sign in as the super_admin (`pdmckinster@gmail.com`).

    ## Test 1 — Success Criterion #1: requests cross-link
    1. Navigate to `/admin/clients`
    2. Click any organization with at least one automation_request (e.g., the Acme seed org)
    3. Open the "Requests" tab on the client detail page
    4. Click the "View all requests" link at the bottom of the requests table
    5. **Expected:** lands on `/admin/requests?org=<slug>` with:
       - URL contains `?org=<slug>` (visible in the address bar)
       - The org filter chip is visible showing the org name (e.g., "Organization: Acme Corp")
       - The requests table is scoped to that org's requests only — compare row count vs the count visible inside the Client 360 tab
    6. Click the ✕ on the chip → URL drops `?org=` but keeps `?status=` if it was present
    7. **Expected:** table now shows ALL requests across all orgs (no scope)

    ## Test 2 — Success Criterion #2: automations cross-link
    Same as Test 1 but starting from the "Automations" tab inside the same client detail, clicking "View all automations", landing on `/admin/automations?org=<slug>`.

    Additionally verify:
    - Combine filters: `/admin/automations?org=<slug>&status=active` should AND-filter to active automations for that org only
    - Clearing the chip with `?status=active` in the URL should preserve `status=active`

    ## Test 3 — Success Criterion #3: slug-or-uuid both work
    - Manually edit the URL to use the org's UUID instead of the slug: `/admin/automations?org=<uuid>`
    - **Expected:** identical filtering behavior, identical chip (same org name displayed)

    ## Test 4 — Success Criterion #4: EN/ES parity
    - Switch locale to Spanish (change the cookie or use the language switcher if present)
    - Repeat Test 1 and Test 2
    - **Expected:** chip label is "Organización:" instead of "Organization:"
    - Navigate to `/admin/requests?org=does-not-exist`
    - **Expected:** destructive chip reads "Organización no encontrada: 'does-not-exist'" (Spanish) or "Organization not found: 'does-not-exist'" (English)

    ## Test 5 — "Organization not found" error state
    - Navigate manually to `/admin/requests?org=garbage-slug-xyz`
    - **Expected:** destructive (red) chip with the localized "Organization not found: 'garbage-slug-xyz'" text + empty table + the rest of the page (tabs, filters) still functional

    ## Sign-off
    Confirm all 5 tests pass in BOTH EN and ES. If anything fails, type the failure description and which test number. Otherwise type "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found in tests 1-5</resume-signal>
</task>

</tasks>

<verification>
- [ ] Both emitter files type-check and use `encodeURIComponent(orgSlug)`
- [ ] `orgSlug` prop traced to `organization.slug` (not UUID)
- [ ] `npm run build` passes from `web/`
- [ ] `npm run lint` passes from `web/`
- [ ] Human UAT: all 5 tests pass in both EN and ES
- [ ] Phase success criteria #1, #2, #3, #4 all observably true
</verification>

<success_criteria>
Phase 23 is complete when the human approves Task 2's UAT. REQUIREMENTS.md traceability flips CLNT-04 and AUTM-02 from "Pending (gap closure)" to "Complete (Phase 23)". Commit message format: `feat(23-03): verify cross-link emitters` (or `fix(23-03): ...` if a divergence was found). After approval, mark the phase complete in ROADMAP.md and update STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/23-client-360-crosslink-fix/23-03-SUMMARY.md` and `.planning/phases/23-client-360-crosslink-fix/23-VERIFICATION.md` (status: passed) with the human's UAT outcomes for each of the 5 tests.
</output>
