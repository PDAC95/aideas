---
phase: 27-scenario-content-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260517000001_functional_areas_seed.sql
autonomous: true
requirements:
  - SCEN-04
must_haves:
  truths:
    - "Querying functional_areas returns exactly 8 active rows: ventas, marketing, atencion-al-cliente, documentos, productividad, reportes, agentes-ia, integraciones-seguridad"
    - "Each functional_area row has non-null EN + ES labels and descriptions"
    - "Running supabase db reset --local from a clean state populates the 8 areas without error"
    - "Re-running the migration on an already-seeded DB produces zero duplicate-key errors and zero net changes (ON CONFLICT DO UPDATE / DO NOTHING is idempotent)"
  artifacts:
    - path: "supabase/migrations/20260517000001_functional_areas_seed.sql"
      provides: "Idempotent INSERT of 8 functional_areas rows keyed by slug"
      contains: "INSERT INTO public.functional_areas"
      contains_also: "ON CONFLICT (slug)"
  key_links:
    - from: "supabase/migrations/20260517000001_functional_areas_seed.sql"
      to: "public.functional_areas"
      via: "INSERT ... ON CONFLICT (slug) DO UPDATE"
      pattern: "ON CONFLICT \\(slug\\)"
    - from: "downstream Plan 27-03 scenarios seed"
      to: "functional_areas rows seeded here"
      via: "subquery lookup by slug to resolve functional_area_id FK"
      pattern: "SELECT id FROM public.functional_areas WHERE slug ="
---

<objective>
Seed the 8 fixed `functional_areas` rows (Ventas, Marketing, Atencion al Cliente, Documentos, Productividad, Reportes, Agentes IA, Integraciones & Seguridad) as the foundation taxonomy that Plan 27-03 will use as the FK target for 50 scenarios.

Purpose: Phase 26 created an empty `functional_areas` table. The public funnel (Phases 29/30) needs these 8 rows present and active so SSR catalog pages render. The seed must be re-runnable so `supabase db reset --local` works on demand during Phase 27/28/29 development.

Output: One new idempotent migration file that inserts the 8 areas keyed by slug. No schema changes. No scenarios, no mappings — those come in Plan 27-03.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scenario-content-seed/27-CONTEXT.md
@.planning/phases/26-catalog-data-model/26-01-SUMMARY.md

<interfaces>
<!-- Schema contract for public.functional_areas (from migration 20260516000001) -->
<!-- Executor MUST insert against these exact columns. Slug is the natural key. -->

```sql
public.functional_areas (
    id              UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    slug            VARCHAR(60)  UNIQUE NOT NULL,
    label_en        VARCHAR(120) NOT NULL,
    label_es        VARCHAR(120) NOT NULL,
    description_en  TEXT,
    description_es  TEXT,
    sort_order      INTEGER NOT NULL DEFAULT 0,
    is_active       BOOLEAN NOT NULL DEFAULT true,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
-- RLS: anon + authenticated SELECT WHERE is_active = true. Writes service_role only.
-- A service-role migration bypasses RLS, so INSERTs run unblocked.
```

The 8 fixed areas (slugs are stable English kebab-case; labels are bilingual; sort_order matches the CONTEXT.md distribution priority):

| sort_order | slug                       | label_en                    | label_es                   |
|------------|----------------------------|-----------------------------|----------------------------|
| 10         | ventas                     | Sales                       | Ventas                     |
| 20         | marketing                  | Marketing                   | Marketing                  |
| 30         | atencion-al-cliente        | Customer Service            | Atencion al Cliente        |
| 40         | documentos                 | Documents                   | Documentos                 |
| 50         | productividad              | Productivity                | Productividad              |
| 60         | reportes                   | Reports                     | Reportes                   |
| 70         | agentes-ia                 | AI Agents                   | Agentes IA                 |
| 80         | integraciones-seguridad    | Integrations & Security     | Integraciones y Seguridad  |

Descriptions: one-sentence EN + ES per area, customer-pain voice (not vendor voice). The executor drafts these inline.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create idempotent functional_areas seed migration</name>
  <files>supabase/migrations/20260517000001_functional_areas_seed.sql</files>
  <action>
Create a NEW migration file at `supabase/migrations/20260517000001_functional_areas_seed.sql`. Never edit existing migrations — this is a brand new file.

The migration MUST:

1. Open with a header comment block following the same style as `20260516000001_functional_areas.sql` (purpose, strategy, idempotency note, downstream consumers — Plan 27-03 scenarios + Plan 27-04 verification).

2. Contain ONE `INSERT INTO public.functional_areas (slug, label_en, label_es, description_en, description_es, sort_order, is_active) VALUES (...) ON CONFLICT (slug) DO UPDATE SET ...` statement covering all 8 rows.

3. Use the exact slugs, labels, and sort_order values from the <interfaces> table above.

4. Author one-sentence bilingual descriptions per area in customer-pain voice. Examples (you may polish wording but keep the tone):
   - ventas: EN "Close more deals and stop losing leads to slow follow-up." / ES "Cierra mas tratos y deja de perder leads por seguimiento lento."
   - marketing: EN "Run campaigns that actually convert without spending your weekend in a spreadsheet." / ES "Lanza campanas que convierten sin pasar el fin de semana en una hoja de calculo."
   - atencion-al-cliente: EN "Reply faster, never drop a ticket, and keep customers happy." / ES "Responde mas rapido, no pierdas tickets y manten contentos a tus clientes."
   - documentos: EN "Stop copy-pasting between forms, contracts, and invoices." / ES "Deja de copiar y pegar entre formularios, contratos y facturas."
   - productividad: EN "Free your team from manual busy-work so they can do real work." / ES "Libera a tu equipo del trabajo manual repetitivo para que haga el trabajo importante."
   - reportes: EN "Get the numbers you actually need without rebuilding the same report every week." / ES "Obten los numeros que necesitas sin reconstruir el mismo reporte cada semana."
   - agentes-ia: EN "Put AI to work answering questions, drafting replies, and routing requests." / ES "Pon a la IA a trabajar respondiendo preguntas, redactando respuestas y enrutando solicitudes."
   - integraciones-seguridad: EN "Connect your tools and keep your data safe across them." / ES "Conecta tus herramientas y manten tus datos seguros entre ellas."

5. The `ON CONFLICT (slug) DO UPDATE` clause MUST update label_en, label_es, description_en, description_es, sort_order, is_active (set is_active = true on conflict to re-activate any soft-deleted rows) and explicitly set `updated_at = NOW()` so the trigger fires. Do NOT update created_at.

6. End the migration with a one-line verification comment like `-- After apply: SELECT count(*) FROM public.functional_areas WHERE is_active = true;  -- expect 8`.

WHY ON CONFLICT DO UPDATE (not DO NOTHING): if Patrick edits labels in a follow-up edit, re-running the migration should push the changes through. DO NOTHING would silently ignore updates. Slug stays the join key so existing FK rows in scenarios (Plan 27-03) survive a label edit.

WHY NO fixed UUIDs: slug is already UNIQUE. Scenarios will look up functional_area_id via `(SELECT id FROM public.functional_areas WHERE slug = '...')`. Stable UUIDs would add 8 hardcoded literals with no benefit.

WHY a migration (not seed.sql): `supabase/seed.sql` is demo data (orgs, profiles, sample executions). Functional_areas are PRODUCT taxonomy — they must exist in production too, so they belong in migrations. This is consistent with Phase 26 patterns.
  </action>
  <verify>
    <automated>cd C:/dev/12ai && npx supabase db reset --local 2>&amp;1 | tail -20 &amp;&amp; docker exec -i supabase_db_12ai psql -U postgres -d postgres -c "SELECT count(*) AS active_areas FROM public.functional_areas WHERE is_active = true; SELECT slug, label_en, label_es, sort_order FROM public.functional_areas ORDER BY sort_order;"</automated>
  </verify>
  <done>
- File `supabase/migrations/20260517000001_functional_areas_seed.sql` exists and is the only new file.
- `npx supabase db reset --local` exits 0.
- `SELECT count(*) FROM public.functional_areas WHERE is_active = true` returns exactly 8.
- All 8 expected slugs are present with non-null label_en, label_es, description_en, description_es.
- sort_order values are 10, 20, 30, 40, 50, 60, 70, 80 in the order listed in the <interfaces> table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove idempotency by re-applying the migration</name>
  <files></files>
  <action>
Prove the seed is re-runnable. Two paths to verify:

1. **Full reset path** — run `npx supabase db reset --local` a SECOND time after Task 1 has already applied. Confirm exit code 0 and that the count stays at 8.

2. **In-place re-apply path** — execute the migration file's INSERT statement directly against the running DB via docker exec / psql. Confirm:
   - Zero `duplicate key value violates unique constraint` errors.
   - Row count remains exactly 8.
   - No new rows inserted (since all 8 slugs already exist).
   - `updated_at` advances on the affected rows (proves ON CONFLICT DO UPDATE fired).

Capture the psql output as evidence in the Plan 27-01 SUMMARY. No new files created in this task — it is verification-only.

WHY both paths: full reset proves the seeded DB is clean; in-place re-apply proves the ON CONFLICT clause works when rows already exist (which is the actual idempotency contract per SCEN-04).
  </action>
  <verify>
    <automated>cd C:/dev/12ai &amp;&amp; npx supabase db reset --local 2>&amp;1 | tail -5 &amp;&amp; docker exec -i supabase_db_12ai psql -U postgres -d postgres -c "SELECT count(*) AS count_after_reset FROM public.functional_areas;" &amp;&amp; docker exec -i supabase_db_12ai psql -U postgres -d postgres -f /docker-entrypoint-initdb.d/migrations/20260517000001_functional_areas_seed.sql 2>&amp;1 | tail -3 &amp;&amp; docker exec -i supabase_db_12ai psql -U postgres -d postgres -c "SELECT count(*) AS count_after_reapply FROM public.functional_areas;"</automated>
  </verify>
  <done>
- `count_after_reset` and `count_after_reapply` both equal 8.
- Zero unique-violation errors in the re-apply output.
- Optional: `SELECT max(updated_at) FROM public.functional_areas` advances on the re-apply (proves DO UPDATE fired).
- Evidence captured for SUMMARY (raw psql output snippets).
  </done>
</task>

</tasks>

<verification>
- `supabase db reset --local` exits 0 from a clean state (Task 1).
- `supabase db reset --local` exits 0 a second time without manual cleanup (Task 2).
- In-place re-apply of the migration file yields zero unique-violation errors (Task 2).
- `SELECT count(*) FROM public.functional_areas WHERE is_active = true` returns exactly 8 in both states.
- All 8 slugs match the <interfaces> table exactly; no typos.
- `npm run build` in `web/` still exits 0 (no schema break for existing authenticated catalog).
</verification>

<success_criteria>
- 8 functional_areas rows seeded, all is_active = true, with bilingual labels + descriptions.
- Migration is idempotent across full-reset AND in-place re-apply.
- No edits to existing migration files, no edits to `supabase/seed.sql`.
- File committed with conventional commit: `feat(27-01): seed 8 functional_areas with idempotent ON CONFLICT clause`.
- SCEN-04 (idempotent seed) demonstrated at the functional_areas layer; will be re-demonstrated at the scenarios layer in Plan 27-03.
</success_criteria>

<output>
After completion, create `.planning/phases/27-scenario-content-seed/27-01-SUMMARY.md` following the template at @C:/Users/patri/.claude/get-shit-done/templates/summary.md.
</output>
