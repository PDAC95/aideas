---
phase: 15-dashboard-home-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md
  - .planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md
  - .planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md
  - .planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md
autonomous: true
requirements: [HOME-01, HOME-02, HOME-04, I18N-01]

must_haves:
  truths:
    - "08-01-SUMMARY.md frontmatter declares which Phase 8 requirement IDs the plan satisfied (HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01) using snake_case key matching the file's existing style"
    - "08-02-SUMMARY.md frontmatter declares which Phase 8 requirement IDs the plan satisfied (HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01) using snake_case key matching the file's existing style"
    - "08-04-SUMMARY.md frontmatter declares the full Phase 8 requirement set verified by UAT (HOME-01..05, NOTF-01..03, I18N-01) using kebab-case key matching the file's minimal-frontmatter style"
    - "08-05-SUMMARY.md frontmatter declares which Phase 8 requirement IDs the plan satisfied (HOME-05, NOTF-02, I18N-01) using snake_case key matching the file's existing style"
    - "Each backfill is a single-line YAML addition — no other frontmatter fields are reformatted, reordered, or rewritten"
  artifacts:
    - path: ".planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md"
      provides: "Phase 8-01 SUMMARY with requirements_completed backfilled"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md"
      provides: "Phase 8-02 SUMMARY with requirements_completed backfilled"
      contains: "requirements_completed:"
    - path: ".planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md"
      provides: "Phase 8-04 SUMMARY with requirements-completed backfilled"
      contains: "requirements-completed:"
    - path: ".planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md"
      provides: "Phase 8-05 SUMMARY with requirements_completed backfilled"
      contains: "requirements_completed:"
  key_links:
    - from: ".planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement IDs (HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01) referenced in frontmatter"
      pattern: "requirements_completed:.*HOME-02"
    - from: ".planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "full Phase 8 requirement IDs verified by UAT referenced in frontmatter"
      pattern: "requirements-completed:.*HOME-01.*NOTF-01"
---

<objective>
Backfill the `requirements_completed` (or `requirements-completed`, depending on each file's existing YAML style) field into the four Phase 8 SUMMARY frontmatter blocks specified by the v1.1 milestone audit. This closes the documentation-gap tech-debt item where Phase 8 plans shipped without explicit requirement-ID traceability in their frontmatter.

Purpose: Closes audit "SUMMARY frontmatter missing" tech-debt for Phase 8. Required for milestone audit traceability so the milestone retrospective can compute requirement coverage by reading frontmatter only (no code spelunking needed).

Output: 4 modified SUMMARY files. Each receives a single new YAML line in its frontmatter. No other frontmatter fields are touched, no body content is changed, no new files are created.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-dashboard-home-polish/15-RESEARCH.md

# The 4 SUMMARY files to backfill (read each to confirm its YAML style before editing):
@.planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md
@.planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md
@.planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md
@.planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md

# Reference style for kebab-case (used by 08-04 backfill):
@.planning/phases/13-catalog-coverage-fix/13-01-SUMMARY.md
@.planning/phases/14-i18n-security-hygiene/14-02-SUMMARY.md

<decisions>
<!-- Locked planner decisions for this plan. Do NOT revisit. -->

1. **Match each file's existing YAML key style.** 08-01, 08-02, 08-05 use `snake_case` keys (e.g., `dependency_graph`, `tech_stack`, `key_files`) — backfill with `requirements_completed:`. 08-04 has minimal frontmatter (only `phase`, `plan`, `status`, `started`, `completed`) — backfill with `requirements-completed:` (kebab-case) to match the newer repo style established by Phase 13 and Phase 14 SUMMARY frontmatter. Do NOT standardize all 4 files to one style — that's a separate concern.

2. **Insertion location: just before `metrics:` (or before the closing `---` if no `metrics:` exists).** Group with other top-level frontmatter fields. Do not insert inside nested structures.

3. **Per-plan requirement attribution (verified against research-recommended values):**
   - `08-01`: i18n keys + types + queries + StatusBadge (data layer + i18n) → `[HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]`
   - `08-02`: KpiCards + AutomationList + ActivityFeed + dashboard page (full home page) → `[HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01]`
   - `08-04`: verification-only plan (UAT covered every requirement) → `[HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NOTF-01, NOTF-02, NOTF-03, I18N-01]`
   - `08-05`: mobile NotificationBell + greeting CTA → `[HOME-05, NOTF-02, I18N-01]`

4. **Do NOT extend scope to 08-03.** The audit names exactly four plans (01, 02, 04, 05). 08-03 (notification bell desktop) already has `provides: ...NotificationBell` in its dependency graph and is NOT in audit scope.

5. **Do NOT modify body content.** This plan ONLY adds a single YAML line to each frontmatter block. Section headings, paragraphs, decision lists, file lists, etc., remain untouched.
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill requirements_completed into 08-01-SUMMARY.md and 08-02-SUMMARY.md (snake_case style)</name>
  <files>.planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md, .planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md</files>
  <action>
For each file, open the YAML frontmatter (between the two `---` lines at the top) and add a single new key.

**08-01-SUMMARY.md:**

1. Locate the frontmatter block (lines 1 to the closing `---`).
2. Insert immediately before the `metrics:` key (or before the closing `---` if `metrics:` is missing) the line:
   ```yaml
   requirements_completed: [HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]
   ```
3. Use the **snake_case** field name (`requirements_completed`) — matches the file's existing style (`dependency_graph`, `tech_stack`, `key_files`).
4. Inline-array syntax `[HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]` (commas between, no quotes — IDs are valid YAML scalars).
5. Do NOT touch any other frontmatter field. Do NOT modify the body content below the closing `---`.

**08-02-SUMMARY.md:**

6. Locate the frontmatter block (lines 1 to the closing `---`).
7. Insert immediately before the `metrics:` key (or before the closing `---` if `metrics:` is missing) the line:
   ```yaml
   requirements_completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01]
   ```
8. Use the **snake_case** field name — matches the file's existing style.
9. Do NOT touch any other frontmatter field. Do NOT modify the body content below the closing `---`.

**Verification per file:** After editing, the frontmatter must still parse as valid YAML (the closing `---` delimiter must remain in place; key indentation must be 0 columns matching siblings).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const m = require('C:/Users/patri/.claude/get-shit-done/node_modules/gray-matter'); for (const f of ['.planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md', '.planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md']) { const d = m(fs.readFileSync(f, 'utf8')); if (!Array.isArray(d.data.requirements_completed)) { console.error('FAIL: ' + f + ' missing requirements_completed array'); process.exit(1); } console.log(f, '->', d.data.requirements_completed.join(',')); }"</automated>
    Fallback (if gray-matter not available globally): `grep -n "requirements_completed:" .planning/phases/08-dashboard-home-notifications/08-01-SUMMARY.md && grep -n "requirements_completed:" .planning/phases/08-dashboard-home-notifications/08-02-SUMMARY.md`
  </verify>
  <done>
- `08-01-SUMMARY.md` frontmatter contains `requirements_completed: [HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]` as a top-level YAML key
- `08-02-SUMMARY.md` frontmatter contains `requirements_completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01]` as a top-level YAML key
- Both files parse as valid YAML+Markdown (gray-matter parses without error)
- Body content of both files is unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Backfill 08-04-SUMMARY.md (kebab-case style for minimal frontmatter) and 08-05-SUMMARY.md (snake_case)</name>
  <files>.planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md, .planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md</files>
  <action>
**08-04-SUMMARY.md (minimal frontmatter, no nested objects — use kebab-case):**

1. Locate the frontmatter block (lines 1-7 in the current file). Current frontmatter has:
   ```yaml
   ---
   phase: 08-dashboard-home-notifications
   plan: 04
   status: complete
   started: 2026-04-10
   completed: 2026-04-10
   ---
   ```
2. Insert immediately before the closing `---` the line:
   ```yaml
   requirements-completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NOTF-01, NOTF-02, NOTF-03, I18N-01]
   ```
3. Use **kebab-case** field name (`requirements-completed`) — matches the newer repo style established by Phase 13 (`13-01-SUMMARY.md:46`) and Phase 14 (`14-02-SUMMARY.md`). The minimal-frontmatter file has no existing snake_case keys to clash with, so kebab-case is the correct choice per locked decision #1.
4. The 9-element array reflects that this plan was a verification-only checkpoint where UAT confirmed all 9 Phase 8 requirements were met.
5. Do NOT modify the body content (the bullet list of verification results).

**08-05-SUMMARY.md (existing snake_case + kebab-case mix — match snake_case for consistency with siblings 08-01/02):**

6. Locate the frontmatter block (lines 1 to the closing `---`). Current file uses snake_case keys (`dependency_graph`, `tech_stack`, `key_files`).
7. Insert immediately before the `metrics:` key (or before the closing `---` if `metrics:` is missing) the line:
   ```yaml
   requirements_completed: [HOME-05, NOTF-02, I18N-01]
   ```
8. Use the **snake_case** field name — matches sibling 08-01, 08-02 plus the file's own existing snake_case keys. Note: the existing `dependency_graph.provides` field already lists `[HOME-05, NOTF-02]` informally; this new top-level field makes it explicit and consumable by audit tooling.
9. Do NOT touch any other frontmatter field. Do NOT modify the body content (decisions list, metrics, etc.).

**Verification per file:** Confirm gray-matter parses both files cleanly.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const m = require('C:/Users/patri/.claude/get-shit-done/node_modules/gray-matter'); const checks = [['.planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md', 'requirements-completed'], ['.planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md', 'requirements_completed']]; for (const [f, k] of checks) { const d = m(fs.readFileSync(f, 'utf8')); if (!Array.isArray(d.data[k])) { console.error('FAIL: ' + f + ' missing ' + k + ' array'); process.exit(1); } console.log(f, '->', k, '=', d.data[k].join(',')); }"</automated>
    Fallback: `grep -n "requirements-completed:" .planning/phases/08-dashboard-home-notifications/08-04-SUMMARY.md && grep -n "requirements_completed:" .planning/phases/08-dashboard-home-notifications/08-05-SUMMARY.md`
  </verify>
  <done>
- `08-04-SUMMARY.md` frontmatter contains `requirements-completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NOTF-01, NOTF-02, NOTF-03, I18N-01]` (kebab-case key, 9-element array)
- `08-05-SUMMARY.md` frontmatter contains `requirements_completed: [HOME-05, NOTF-02, I18N-01]` (snake_case key, 3-element array)
- Both files parse as valid YAML+Markdown (gray-matter parses without error)
- Body content of both files is unchanged
- 08-03-SUMMARY.md is NOT modified (out of audit scope)
  </done>
</task>

</tasks>

<verification>
After both tasks:

1. **YAML validity:** All 4 SUMMARY files parse as valid frontmatter+markdown via gray-matter:
   ```bash
   for f in 08-01 08-02 08-04 08-05; do
     node -e "const m = require('gray-matter'); m(require('fs').readFileSync('.planning/phases/08-dashboard-home-notifications/$f-SUMMARY.md','utf8'));" || echo "FAIL: $f"
   done
   ```
2. **Field presence:**
   - `08-01`: has `requirements_completed` (snake_case)
   - `08-02`: has `requirements_completed` (snake_case)
   - `08-04`: has `requirements-completed` (kebab-case)
   - `08-05`: has `requirements_completed` (snake_case)
3. **Array contents (verbatim per locked decision #3):**
   - 08-01: `[HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]`
   - 08-02: `[HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01]`
   - 08-04: `[HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NOTF-01, NOTF-02, NOTF-03, I18N-01]`
   - 08-05: `[HOME-05, NOTF-02, I18N-01]`
4. **Untouched files:** `08-03-SUMMARY.md` is NOT modified (verify with `git status` showing only the 4 target files changed in `.planning/phases/08-dashboard-home-notifications/`).
5. **Body untouched:** `git diff --stat` for each of the 4 files shows only +1 line, 0 deletions (or +1 line if a trailing newline is also added).
</verification>

<success_criteria>
- [ ] 08-01-SUMMARY.md frontmatter has `requirements_completed: [HOME-02, HOME-03, HOME-04, NOTF-01, I18N-01]`
- [ ] 08-02-SUMMARY.md frontmatter has `requirements_completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, I18N-01]`
- [ ] 08-04-SUMMARY.md frontmatter has `requirements-completed: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, NOTF-01, NOTF-02, NOTF-03, I18N-01]`
- [ ] 08-05-SUMMARY.md frontmatter has `requirements_completed: [HOME-05, NOTF-02, I18N-01]`
- [ ] All 4 files parse as valid YAML+Markdown
- [ ] 08-03-SUMMARY.md is NOT modified
- [ ] Body content of all 4 files is unchanged (only frontmatter lines added)
</success_criteria>

<output>
After completion, create `.planning/phases/15-dashboard-home-polish/15-02-SUMMARY.md` documenting:
- Files modified (4 SUMMARY files)
- Locked decisions enacted (per-file YAML style match: snake_case for 08-01/02/05, kebab-case for 08-04)
- Audit tech-debt closed (Phase 8 SUMMARY frontmatter traceability gap)
- Frontmatter MUST include `requirements-completed: [HOME-01, HOME-02, HOME-04, I18N-01]` (kebab-case style, matching Phase 13/14 SUMMARY pattern)
</output>
