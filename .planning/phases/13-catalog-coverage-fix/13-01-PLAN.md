---
phase: 13-catalog-coverage-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/messages/en.json
  - web/messages/es.json
  - web/src/components/dashboard/catalog-client.tsx
  - web/src/app/(dashboard)/dashboard/catalog/page.tsx
autonomous: false
requirements:
  - CATL-01
  - CATL-02
  - CATL-03
  - I18N-01

must_haves:
  truths:
    - "User clicking 'Operations' (EN) / 'Operaciones' (ES) tab on /dashboard/catalog sees exactly 8 template cards"
    - "User clicking 'Agencies' (EN) / 'Agencias' (ES) chip on /dashboard/catalog sees exactly 48 template cards"
    - "Switching locale EN<->ES via the header LanguageSwitcher renders 'Operations'/'Operaciones' and 'Agencies'/'Agencias' labels correctly"
    - "Selecting Agencias chip + Operations tab simultaneously renders a non-empty intersection (seed has data_reconciliation, workflow_orchestrator, system_health_monitor, backup_verification)"
    - "Mobile width (375px) tab strip renders all 9 category tabs without layout regression (uses existing horizontal-scroll pattern)"
    - "Template detail pages for operations templates render the localized 'Operations'/'Operaciones' label in the category badge (auto-resolves via existing dynamic t() lookup in [slug]/page.tsx)"
  artifacts:
    - path: "web/messages/en.json"
      provides: "EN i18n keys for new category and industry"
      contains: '"operations": "Operations"'
    - path: "web/messages/en.json"
      provides: "EN industry key"
      contains: '"agencias": "Agencies"'
    - path: "web/messages/es.json"
      provides: "ES i18n keys for new category and industry"
      contains: '"operations": "Operaciones"'
    - path: "web/messages/es.json"
      provides: "ES industry key"
      contains: '"agencias": "Agencias"'
    - path: "web/src/components/dashboard/catalog-client.tsx"
      provides: "operations entry registered in CATEGORY_ORDER between productivity and reports"
      contains: '"operations"'
    - path: "web/src/app/(dashboard)/dashboard/catalog/page.tsx"
      provides: "operations and agencias entries in server-to-client translations map"
      contains: 'operations: t("categories.operations")'
  key_links:
    - from: "web/src/components/dashboard/catalog-client.tsx"
      to: "web/src/app/(dashboard)/dashboard/catalog/page.tsx"
      via: "translations.categories.operations prop must resolve to a non-empty string"
      pattern: 'translations\.categories\[key\]'
    - from: "web/src/app/(dashboard)/dashboard/catalog/page.tsx"
      to: "web/messages/en.json + web/messages/es.json"
      via: 'getTranslations() resolves t("categories.operations") and t("industries.agencias")'
      pattern: 't\("(categories|industries)\.(operations|agencias)"\)'
    - from: "Object.keys(translations.industries) in catalog-client.tsx"
      to: "industry chips render order"
      via: "JSON insertion order = render order; agencias must be appended LAST in both en.json and es.json industries blocks for consistent EN/ES chip layout"
      pattern: '"agencias":'
---

<objective>
Register the orphaned `operations` category and `agencias` industry into the catalog UI so all 66 seeded `automation_templates` are reachable via filters. Closes audit gaps HIGH-1 (`operations`, 8 templates) and HIGH-2 (`agencias`, 48 templates) from `.planning/v1.1-MILESTONE-AUDIT.md`.

Purpose: Two seed values exist in DB and are reachable via URL query params, but they're invisible in the UI because three registration points (i18n JSON keys, `CATEGORY_ORDER` array, and the explicit translations map in `catalog/page.tsx`) lack the entries. This plan adds those entries plus a manual smoke test gate.

Output: 4 files modified, 1 new tab + 1 new chip rendered in catalog, manual UAT pass on EN+ES + mobile.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-catalog-coverage-fix/13-CONTEXT.md
@.planning/phases/13-catalog-coverage-fix/13-RESEARCH.md
@web/messages/en.json
@web/messages/es.json
@web/src/components/dashboard/catalog-client.tsx
@web/src/app/(dashboard)/dashboard/catalog/page.tsx

<interfaces>
<!-- Verified current state (2026-04-30). All four target sites confirmed before planning. -->

From web/messages/en.json (lines 324-342) — current state, NO operations / agencias keys yet:
```json
"categories": {
  "all": "All",
  "mas_populares": "Most popular",
  "sales": "Sales",
  "marketing": "Marketing",
  "customer_service": "Customer service",
  "documents": "Documents",
  "productivity": "Productivity",
  "reports": "Reports",
  "ai_agents": "AI Agents"
},
"industries": {
  "all": "All",
  "retail": "Retail",
  "salud": "Health",
  "legal": "Legal",
  "inmobiliaria": "Real estate",
  "restaurantes": "Restaurants"
}
```

From web/messages/es.json (lines 324-342) — current state, NO operations / agencias keys yet:
```json
"categories": {
  "all": "Todas",
  "mas_populares": "Más populares",
  "sales": "Ventas",
  "marketing": "Marketing",
  "customer_service": "Atención al cliente",
  "documents": "Documentos",
  "productivity": "Productividad",
  "reports": "Reportes",
  "ai_agents": "Agentes IA"
},
"industries": {
  "all": "Todas",
  "retail": "Retail",
  "salud": "Salud",
  "legal": "Legal",
  "inmobiliaria": "Inmobiliaria",
  "restaurantes": "Restaurantes"
}
```

From web/src/components/dashboard/catalog-client.tsx (lines 32-42) — current state:
```typescript
const CATEGORY_ORDER = [
  "all",
  "mas_populares",
  "sales",
  "marketing",
  "customer_service",
  "documents",
  "productivity",
  "reports",
  "ai_agents",
];
```

From web/src/app/(dashboard)/dashboard/catalog/page.tsx (lines 62-80) — current state:
```typescript
categories: {
  all: t("categories.all"),
  mas_populares: t("categories.mas_populares"),
  sales: t("categories.sales"),
  marketing: t("categories.marketing"),
  customer_service: t("categories.customer_service"),
  documents: t("categories.documents"),
  productivity: t("categories.productivity"),
  reports: t("categories.reports"),
  ai_agents: t("categories.ai_agents"),
},
industries: {
  all: t("industries.all"),
  retail: t("industries.retail"),
  salud: t("industries.salud"),
  legal: t("industries.legal"),
  inmobiliaria: t("industries.inmobiliaria"),
  restaurantes: t("industries.restaurantes"),
},
```

How filtering works (verified, no changes needed):
- catalog-client.tsx:128-130 derives industry chips from `Object.keys(translations.industries)` — JSON insertion order = chip render order. Therefore `"agencias"` MUST be appended as the LAST key in BOTH en.json and es.json `industries` blocks (any other position causes EN/ES chip layout mismatch).
- catalog-client.tsx:187-189 maps `CATEGORY_ORDER` and renders a tab only if `translations.categories[key]` resolves (`if (!label) return null`). Both registrations are required.
- catalog-client.tsx:140 filters `t.category !== category` — exact-string match. Already works for `"operations"` once the tab is visible.
- catalog/[slug]/page.tsx:91-97 does dynamic `t(\`categories.${template.category}\`)` with try/catch fallback — operations detail pages auto-resolve once the JSON key exists. NO EDIT to [slug]/page.tsx.
- supabase seed.sql verified: 8 rows with `category = 'operations'` (lines 717-827), 48 rows containing `'agencias'` in `industry_tags` array literal.
- Schema CHECK constraint already permits `'operations'` (migration 20260409000001_v1_1_schema_expansion.sql:46-49).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register operations category and agencias industry in i18n + UI registration points</name>
  <files>
    web/messages/en.json
    web/messages/es.json
    web/src/components/dashboard/catalog-client.tsx
    web/src/app/(dashboard)/dashboard/catalog/page.tsx
  </files>
  <action>
Apply four mechanical edits in order (read each file first, then use Edit tool):

**Edit 1 — web/messages/en.json** (lines 324-342, `dashboard.catalog` block):
- In the `categories` object, insert `"operations": "Operations",` between `"productivity": "Productivity",` and `"reports": "Reports"`. Add the trailing comma to the productivity line (it already has one — preserve it). Result: 10 category keys total.
- In the `industries` object, append `"agencias": "Agencies"` as the LAST key after `"restaurantes": "Restaurants"`. Add a trailing comma to the restaurantes line. Result: 7 industry keys total.

**Edit 2 — web/messages/es.json** (lines 324-342, mirror structure):
- In the `categories` object, insert `"operations": "Operaciones",` between `"productivity": "Productividad",` and `"reports": "Reportes"`.
- In the `industries` object, append `"agencias": "Agencias"` as the LAST key after `"restaurantes": "Restaurantes"`.

**Edit 3 — web/src/components/dashboard/catalog-client.tsx** (lines 32-42, `CATEGORY_ORDER` constant):
Insert `"operations",` between `"productivity",` and `"reports",`. New array:
```typescript
const CATEGORY_ORDER = [
  "all",
  "mas_populares",
  "sales",
  "marketing",
  "customer_service",
  "documents",
  "productivity",
  "operations",
  "reports",
  "ai_agents",
];
```

**Edit 4 — web/src/app/(dashboard)/dashboard/catalog/page.tsx** (lines 62-80, `translations` object literal):
- In the `categories` object, insert `operations: t("categories.operations"),` between `productivity: t("categories.productivity"),` and `reports: t("categories.reports"),`.
- In the `industries` object, append `agencias: t("industries.agencias"),` as the LAST entry after `restaurantes: t("industries.restaurantes"),`. Add a trailing comma to the restaurantes line if not already present.

**CRITICAL constraints (from RESEARCH.md pitfalls — do NOT deviate):**
1. **JSON commas:** When inserting a new key in the middle of an object, the prior line MUST end in a comma. Verify visually after edit. Forgetting it breaks the entire i18n bundle and crashes all dashboard pages with `SyntaxError: Unexpected string in JSON`.
2. **EN/ES industry order parity:** `"agencias"` MUST be the LAST key in BOTH en.json and es.json `industries` blocks. `Object.keys(translations.industries)` reflects insertion order, so a mismatch causes chips to render in different positions per locale.
3. **DO NOT touch:** `catalog/[slug]/page.tsx` (dynamic t() with try/catch already handles new keys), `catalog-card.tsx` (raw category/industry string rendering already works), `queries.ts` (already returns all templates), `types.ts` (string types compatible), seed data (counts already correct), schema migrations (CHECK constraint already permits operations).
4. **DO NOT redesign the tab strip.** Existing `overflow-x-auto pb-2 scrollbar-hide` (catalog-client.tsx:186) handles 9 tabs on mobile. If the 9th tab causes a regression, fix minimally (per CONTEXT.md discretion); do not refactor breakpoints.
5. **DO NOT alphabetize JSON keys** — match `CATEGORY_ORDER` (operations after productivity) per RESEARCH.md recommendation.

**Validate immediately after edits (before moving to verify):**
```bash
node -e "JSON.parse(require('fs').readFileSync('web/messages/en.json','utf8'))"
node -e "JSON.parse(require('fs').readFileSync('web/messages/es.json','utf8'))"
```
Both must return without error. If `SyntaxError` appears, fix the missing comma before proceeding.
  </action>
  <verify>
    <automated>
cd web && node -e "const en=JSON.parse(require('fs').readFileSync('messages/en.json','utf8'));const es=JSON.parse(require('fs').readFileSync('messages/es.json','utf8'));const cat=en.dashboard.catalog.categories;const ind=en.dashboard.catalog.industries;const escat=es.dashboard.catalog.categories;const esind=es.dashboard.catalog.industries;if(cat.operations!=='Operations')throw new Error('en.json missing operations=Operations, got: '+cat.operations);if(ind.agencias!=='Agencies')throw new Error('en.json missing agencias=Agencies, got: '+ind.agencias);if(escat.operations!=='Operaciones')throw new Error('es.json missing operations=Operaciones, got: '+escat.operations);if(esind.agencias!=='Agencias')throw new Error('es.json missing agencias=Agencias, got: '+esind.agencias);const enIndKeys=Object.keys(ind);const esIndKeys=Object.keys(esind);if(enIndKeys[enIndKeys.length-1]!=='agencias')throw new Error('en.json: agencias must be LAST in industries, got order: '+enIndKeys.join(','));if(esIndKeys[esIndKeys.length-1]!=='agencias')throw new Error('es.json: agencias must be LAST in industries, got order: '+esIndKeys.join(','));if(JSON.stringify(enIndKeys)!==JSON.stringify(esIndKeys))throw new Error('EN/ES industry key ORDER MISMATCH. EN: '+enIndKeys.join(',')+' | ES: '+esIndKeys.join(','));console.log('JSON OK: operations + agencias registered in EN+ES with matching industry order')" && grep -n '"operations"' src/components/dashboard/catalog-client.tsx && grep -n 'operations: t("categories.operations")' src/app/\(dashboard\)/dashboard/catalog/page.tsx && grep -n 'agencias: t("industries.agencias")' src/app/\(dashboard\)/dashboard/catalog/page.tsx && npm run lint && npm run build
    </automated>
  </verify>
  <done>
- en.json has `dashboard.catalog.categories.operations = "Operations"` and `dashboard.catalog.industries.agencias = "Agencies"` (agencias is the LAST industry key)
- es.json has `dashboard.catalog.categories.operations = "Operaciones"` and `dashboard.catalog.industries.agencias = "Agencias"` (agencias is the LAST industry key)
- EN and ES `industries` key order is byte-for-byte identical
- catalog-client.tsx `CATEGORY_ORDER` contains `"operations"` between `"productivity"` and `"reports"` (10 entries total)
- catalog/page.tsx `translations.categories` contains `operations: t("categories.operations")` and `translations.industries` contains `agencias: t("industries.agencias")` as last entry
- `npm run lint` exits 0
- `npm run build` exits 0 (no TypeScript errors, no JSON parse errors, no next-intl missing-key warnings for the four new keys)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
4 files updated to register the orphaned `operations` category (8 seeded templates) and `agencias` industry (48 seeded templates) into the catalog UI:
- `web/messages/en.json` — added `categories.operations = "Operations"` and `industries.agencias = "Agencies"`
- `web/messages/es.json` — added `categories.operations = "Operaciones"` and `industries.agencias = "Agencias"`
- `web/src/components/dashboard/catalog-client.tsx` — added `"operations"` to `CATEGORY_ORDER` between `productivity` and `reports`
- `web/src/app/(dashboard)/dashboard/catalog/page.tsx` — added `operations` and `agencias` entries to the server-to-client translations map

Already automated and passing:
- JSON parses valid in both files
- EN/ES industry key order matches byte-for-byte
- `npm run lint` clean
- `npm run build` clean
  </what-built>
  <how-to-verify>
Manual smoke test in dev (per CONTEXT.md UAT scope):

**Setup:**
1. From `c:/dev/12ai/web` run `npm run dev` (if not already running on port 3000).
2. Open `http://localhost:3000/dashboard/catalog` in your browser. Log in if needed.

**Test 1 — Operations tab (ES locale):**
3. Ensure the language switcher in the header shows ES (or click it to switch to Español).
4. In the catalog tabs strip, click **Operaciones** (it should appear between **Productividad** and **Reportes**).
5. Confirm the grid renders **exactly 8 template cards**. The "Mostrando X de 66" subtitle should read "Mostrando 8 de 66" (or equivalent).
6. Tab label must read **Operaciones** (not "categories.operations" raw key, not "Operations" English).

**Test 2 — Agencias chip (ES locale):**
7. Click the **Todas** tab to clear the category filter.
8. In the industry chip row, click **Agencias** (it should be the LAST chip after **Restaurantes**).
9. Confirm the grid renders **48 template cards**. The "Mostrando X de 66" subtitle should read "Mostrando 48 de 66".

**Test 3 — Combined filter:**
10. With **Agencias** still selected, click the **Operations** / **Operaciones** tab.
11. Confirm the result is **non-empty** — at minimum, these 4 templates should appear: `data_reconciliation`, `workflow_orchestrator`, `system_health_monitor`, `backup_verification` (search by their card titles or hover for the slug).

**Test 4 — Locale toggle (EN):**
12. Open the language switcher in the header and select **English**.
13. Confirm the tab label now reads **Operations** (not "Operaciones") and the chip label reads **Agencies** (not "Agencias").
14. Click **Operations** tab — counts should still match (8 cards).
15. Click **Agencies** chip — counts should still match (48 cards).

**Test 5 — Mobile width:**
16. Open Chrome DevTools (F12) → device toolbar → set viewport to **375px wide** (iPhone SE preset).
17. Reload the catalog page. Confirm the tab strip horizontally scrolls without overlapping the chip row, sidebar, or other content.
18. Confirm all 9 tabs reachable by horizontal scroll (no clipped layout, no broken wrapping).

**Test 6 — Template detail page (operations):**
19. From the Operations tab, click any operations template card to open its detail page.
20. Confirm the category badge near the title reads **Operations** (EN) or **Operaciones** (ES) — not the raw key.

**Pass criteria (all must be true):**
- Counts match: 8 operations, 48 agencias.
- Labels render in the correct locale (no raw keys visible anywhere).
- EN and ES chip strip orders are visually identical.
- Mobile 375px does not regress (horizontal scroll works, no overlap).
- Template detail page for an operations template shows the localized category label.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 tests pass, or describe which test failed and what you saw.</resume-signal>
</task>

</tasks>

<verification>
**Automated (in Task 1):**
- JSON parse validity for en.json and es.json
- Required keys present and correctly valued in both locales
- agencias is last industry key in both files
- EN/ES industry key order match
- CATEGORY_ORDER contains operations
- catalog/page.tsx translations map contains both new entries
- `npm run lint` clean
- `npm run build` clean

**Manual (in checkpoint):**
- 6-test UAT smoke test covering EN+ES locale, count verification (8 ops, 48 agencias), combined filter intersection, mobile 375px layout, and template detail page localization.
</verification>

<success_criteria>
1. `dashboard.catalog.categories.operations` key present in both `web/messages/en.json` (value "Operations") and `web/messages/es.json` (value "Operaciones")
2. `dashboard.catalog.industries.agencias` key present in both `web/messages/en.json` (value "Agencies") and `web/messages/es.json` (value "Agencias"), as the LAST industry key in both files
3. `CATEGORY_ORDER` in `catalog-client.tsx` includes `"operations"` after `"productivity"` and before `"reports"` (10 entries total)
4. `catalog/page.tsx` translations map includes `operations: t("categories.operations")` in `categories` and `agencias: t("industries.agencias")` as last entry in `industries`
5. Browsing `/dashboard/catalog` and selecting **Operations** tab (EN) or **Operaciones** tab (ES) shows exactly 8 template cards
6. Selecting **Agencies** (EN) / **Agencias** (ES) chip shows exactly 48 template cards
7. EN/ES locale toggle re-renders the labels in the correct language
8. Mobile 375px width tab strip horizontally scrolls without layout regression
9. Template detail page for an operations template renders the localized category label (auto-handled, no code change required)
10. `npm run build` and `npm run lint` exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-catalog-coverage-fix/13-01-SUMMARY.md` documenting:
- Exact diff applied to each of the 4 files
- Final EN+ES JSON key counts (10 categories, 7 industries each, with matching key order)
- Counts verified during UAT (8 operations templates, 48 agencias templates)
- Confirmation that no other files were touched (especially `[slug]/page.tsx`, `catalog-card.tsx`, queries.ts, schema)
- requirements_completed: [CATL-01, CATL-02, CATL-03, I18N-01]
- Audit gap closure note: "Closes HIGH-1 and HIGH-2 from .planning/v1.1-MILESTONE-AUDIT.md"
</output>
