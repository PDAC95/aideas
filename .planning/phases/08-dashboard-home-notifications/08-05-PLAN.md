---
phase: 08-dashboard-home-notifications
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - web/src/components/dashboard/nav.tsx
  - web/src/app/(dashboard)/dashboard/page.tsx
autonomous: true
requirements: [HOME-05, NOTF-02]

must_haves:
  truths:
    - "Mobile notification row in nav.tsx is interactive — clicking it opens the NotificationBell popover so mobile users can view and manage notifications"
    - "Greeting row in page.tsx contains a '+ New automation' Link visible on mobile viewports, linking to /dashboard/catalog"
  artifacts:
    - web/src/components/dashboard/nav.tsx
    - web/src/app/(dashboard)/dashboard/page.tsx
  key_links:
    - from: web/src/components/dashboard/nav.tsx
      to: web/src/components/dashboard/notification-bell.tsx
      via: "imports and renders NotificationBell component in mobile menu"
---

<objective>
Fix two verification gaps from 08-VERIFICATION.md:

1. **NOTF-02 (mobile notifications):** Replace the static `<div>` for mobile notifications in nav.tsx (lines 124-133) with the existing `NotificationBell` component so mobile users can open the notification dropdown and mark notifications as read.

2. **HOME-05 (mobile CTA):** Add a `+ New automation` Link in the greeting row of page.tsx (lines 157-163) so mobile users have a prominent header-level entry point to `/dashboard/catalog`. The Link should be visible at all viewport sizes.

These are small, targeted fixes. No new components or files are created.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-dashboard-home-notifications/08-VERIFICATION.md
@web/src/components/dashboard/nav.tsx
@web/src/components/dashboard/notification-bell.tsx
@web/src/app/(dashboard)/dashboard/page.tsx
@web/src/components/dashboard/dashboard-header.tsx
</context>

<tasks>

<task type="auto" id="gap-fix-mobile-notifications">
  <files>
    web/src/components/dashboard/nav.tsx
  </files>
  <action>
In nav.tsx, replace the static notifications `<div>` block (lines 124-133) with the `NotificationBell` component that already exists at `@/components/dashboard/notification-bell`.

Steps:
1. Add import: `import { NotificationBell } from "@/components/dashboard/notification-bell";`
2. Replace the static `<div>` at lines 124-133 (the one with `<Bell>` icon, `tNotif("title")` text, and the red unread badge) with a wrapper that renders the NotificationBell with its full prop interface:
   ```tsx
   <NotificationBell
     initialNotifications={notifications}
     unreadCount={unreadCount}
     userId={user.id}
     translations={{
       title: tNotif("title"),
       markAllRead: tNotif("markAllRead"),
       empty: tNotif("empty"),
     }}
   />
   ```
   Note: `unreadCount` is already computed in nav.tsx as `const unreadCount = notifications.filter(n => !n.is_read).length`. The `user` prop is declared in `DashboardNavProps` — ensure it is destructured in the function signature (e.g., `{ user, notifications, ... }`).
3. The NotificationBell component is a "use client" component that manages its own state (Radix Popover). It accepts `initialNotifications`, `unreadCount`, `userId`, and `translations` as required props.
4. Wrap the NotificationBell in a `<div>` with the same padding/layout classes as neighboring mobile menu items (`px-4 py-3`) so it aligns visually with the rest of the mobile menu.
5. Keep the border-t divider above the notifications row.
6. Do NOT remove the `Bell` import from lucide-react if other parts of nav.tsx still use it; otherwise clean it up.

The goal: mobile users tap the notification row in the hamburger menu → the NotificationBell Radix popover opens showing the last 20 notifications with type icons, messages, unread blue dots, and "Mark all as read" action.
  </action>
  <verify>
    <automated>cd /c/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Mobile hamburger menu renders NotificationBell component instead of a static div. TypeScript compiles without errors. Tapping the notification row on mobile opens the popover with notification list and mark-all-read action.</done>
</task>

<task type="auto" id="gap-fix-mobile-cta">
  <files>
    web/src/app/(dashboard)/dashboard/page.tsx
  </files>
  <action>
In page.tsx, add a `+ New automation` Link button in the greeting row (the `<div className="mb-6">` block around lines 158-163) so mobile users see a prominent CTA.

Steps:
1. Import `Link` from `next/link` if not already imported, and `Plus` from `lucide-react` if not already imported.
2. Modify the greeting `<div className="mb-6">` to use a flex layout: `<div className="mb-6 flex items-start justify-between gap-4">`.
3. Wrap the existing `<h1>` and `<p>` in a `<div>` so they stay grouped on the left.
4. Add a Link button on the right side:
   ```tsx
   <Link
     href="/dashboard/catalog"
     className="shrink-0 inline-flex items-center gap-1.5 rounded-lg bg-purple-600 px-3 py-2 text-sm font-medium text-white hover:bg-purple-700 transition-colors"
   >
     <Plus className="h-4 w-4" />
     <span className="hidden sm:inline">{t("home.newAutomation")}</span>
     <span className="sm:hidden">{t("home.newAutomationShort")}</span>
   </Link>
   ```
5. Add the translation keys if missing:
   - In `web/messages/en.json` under `dashboard.home`: `"newAutomation": "New automation"` and `"newAutomationShort": "New"`
   - In `web/messages/es.json` under `dashboard.home`: `"newAutomation": "Nueva automatización"` and `"newAutomationShort": "Nueva"`
6. The button should be visible at ALL viewport sizes. On mobile (< sm), show just the icon + "New" short label. On sm+, show "New automation" full label.
7. Use purple-600 to match the existing accent color used throughout the dashboard.

The goal: mobile users see a prominent "+" button in the greeting area to quickly navigate to the catalog page.
  </action>
  <verify>
    <automated>cd /c/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Greeting row in page.tsx has a purple "+ New automation" Link visible at all viewports. On mobile it shows a compact label; on desktop the full text. Links to /dashboard/catalog. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` passes with zero errors in web/ directory
2. Mobile viewport (< 768px): hamburger menu notifications row opens a popover with notification list on tap
3. Mobile viewport (< 768px): greeting area shows a purple "+" CTA button linking to /dashboard/catalog
4. Desktop: no visual regressions — DashboardHeader CTA and NotificationBell still function normally
</verification>
