---
phase: 05-user-login
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/(auth)/login/page.tsx
  - web/src/components/auth/login-form.tsx
  - web/src/lib/validations/login.ts
  - web/messages/en.json
  - web/messages/es.json
autonomous: true
requirements: [LOGIN-01, LOGIN-04, LOGIN-05, LOGIN-06]

must_haves:
  truths:
    - "Login page at /login renders a split layout matching the signup page design"
    - "Form has email, password (with show/hide toggle), 'Remember me' checkbox, and submit button"
    - "Google OAuth button appears below form fields with divider, same position as signup"
    - "'Forgot password' link is visible below password field, right-aligned"
    - "'Don't have an account? Sign up' link is visible below the login button"
    - "Language switcher (ES|EN) is visible in top-right corner"
    - "All text is translatable via login namespace in en.json and es.json"
    - "Form validates email format and non-empty password via Zod before submission"
  artifacts:
    - path: "web/src/app/(auth)/login/page.tsx"
      provides: "Server Component login page with split layout"
      contains: "getTranslations"
    - path: "web/src/components/auth/login-form.tsx"
      provides: "Client component with react-hook-form + Zod login form"
      contains: "useForm"
    - path: "web/src/lib/validations/login.ts"
      provides: "Zod schema for login form"
      exports: ["loginSchema", "LoginFormData"]
    - path: "web/messages/en.json"
      provides: "English translations including login namespace"
      contains: "login"
    - path: "web/messages/es.json"
      provides: "Spanish translations including login namespace"
      contains: "login"
  key_links:
    - from: "web/src/app/(auth)/login/page.tsx"
      to: "web/src/components/auth/login-form.tsx"
      via: "import and render"
      pattern: "LoginForm"
    - from: "web/src/components/auth/login-form.tsx"
      to: "web/src/lib/validations/login.ts"
      via: "zodResolver import"
      pattern: "loginSchema"
---

<objective>
Build the login page UI with split layout, form fields, Zod validation schema, Google OAuth button, and bilingual i18n translations. This replaces the existing placeholder login page with a production-quality design matching the signup page.

Purpose: Delivers LOGIN-01 (login page with form), LOGIN-04 (error message display infrastructure), LOGIN-05 (forgot password link), LOGIN-06 (Google OAuth button). Plan 02 wires the actual auth logic.
Output: Login page renders with full form, validation, i18n, and visual parity with signup page.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@web/src/app/(auth)/signup/page.tsx
@web/src/components/auth/signup-form.tsx
@web/src/components/auth/google-oauth-button.tsx
@web/src/components/auth/language-switcher.tsx
@web/src/lib/validations/signup.ts
@web/messages/en.json
@web/messages/es.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From web/src/components/auth/google-oauth-button.tsx:
```typescript
interface GoogleOAuthButtonProps {
  redirectTo?: string;  // defaults to "/complete-registration"
  className?: string;
}
export function GoogleOAuthButton({ redirectTo, className }: GoogleOAuthButtonProps)
// Uses useTranslations("signup") for button text — login form must pass a translationNamespace prop OR the component needs updating
// NOTE: GoogleOAuthButton currently uses useTranslations("signup") for its label text.
// On the login page, pass redirectTo="/dashboard" and use the component as-is.
// The button text says "Continue with Google" from the signup namespace — acceptable for login too.
```

From web/src/components/auth/language-switcher.tsx:
```typescript
export function LanguageSwitcher()
// No props — self-contained, reads/writes locale cookie
```

From web/src/lib/validations/signup.ts (pattern to follow):
```typescript
import { z } from 'zod'
export const signupSchema = z.object({ ... })
export type SignupFormData = z.infer<typeof signupSchema>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create login Zod schema and i18n translations</name>
  <files>
    web/src/lib/validations/login.ts
    web/messages/en.json
    web/messages/es.json
  </files>
  <action>
1. Create `web/src/lib/validations/login.ts` with a Zod schema:
   ```typescript
   import { z } from 'zod'

   export const loginSchema = z.object({
     email: z
       .string()
       .min(1, 'Email is required')
       .email('Invalid email address')
       .transform((val) => val.toLowerCase().trim()),
     password: z.string().min(1, 'Password is required'),
     rememberMe: z.boolean().optional(),
   })

   export type LoginFormData = z.input<typeof loginSchema>
   ```
   NOTE: Use `z.input` for the form type (not `z.infer`) because the email `transform` creates an input/output type mismatch with react-hook-form — same pattern issue discovered in Phase 4 (04-02 decision).

2. Add a `"login"` namespace to `web/messages/en.json` alongside existing namespaces. Keys:
   ```json
   "login": {
     "title": "Welcome back",
     "subtitle": "Sign in to your account",
     "email": "Email",
     "emailPlaceholder": "you@company.com",
     "password": "Password",
     "passwordPlaceholder": "Enter your password",
     "rememberMe": "Remember me",
     "forgotPassword": "Forgot your password?",
     "submit": "Sign in",
     "submitting": "Signing in...",
     "noAccount": "Don't have an account?",
     "register": "Sign up",
     "or": "or",
     "errors": {
       "invalidCredentials": "Invalid email or password.",
       "emailNotVerified": "Please verify your email before signing in.",
       "tooManyAttempts": "Too many attempts. Try again in {minutes} minutes.",
       "generic": "Something went wrong. Please try again.",
       "authError": "Authentication failed. Please try again."
     },
     "sessionExpired": "Your session has expired. Please sign in again.",
     "branding": {
       "headline": "Welcome back",
       "subheadline": "Your automations are running. Check in on your business.",
       "highlight1": "Real-time automation monitoring",
       "highlight2": "Track ROI and time saved",
       "highlight3": "Collaborate with your team"
     }
   }
   ```

3. Add matching `"login"` namespace to `web/messages/es.json` with Spanish translations:
   ```json
   "login": {
     "title": "Bienvenido de nuevo",
     "subtitle": "Inicia sesion en tu cuenta",
     "email": "Correo electronico",
     "emailPlaceholder": "tu@empresa.com",
     "password": "Contrasena",
     "passwordPlaceholder": "Ingresa tu contrasena",
     "rememberMe": "Recordarme",
     "forgotPassword": "Olvidaste tu contrasena?",
     "submit": "Iniciar sesion",
     "submitting": "Iniciando sesion...",
     "noAccount": "No tienes cuenta?",
     "register": "Registrate",
     "or": "o",
     "errors": {
       "invalidCredentials": "Email o contrasena incorrectos.",
       "emailNotVerified": "Verifica tu correo antes de iniciar sesion.",
       "tooManyAttempts": "Demasiados intentos. Intentalo en {minutes} minutos.",
       "generic": "Algo salio mal. Intentalo de nuevo.",
       "authError": "Error de autenticacion. Intentalo de nuevo."
     },
     "sessionExpired": "Tu sesion ha expirado. Inicia sesion de nuevo.",
     "branding": {
       "headline": "Bienvenido de nuevo",
       "subheadline": "Tus automatizaciones estan activas. Revisa tu negocio.",
       "highlight1": "Monitoreo en tiempo real",
       "highlight2": "Mide el ROI y tiempo ahorrado",
       "highlight3": "Colabora con tu equipo"
     }
   }
   ```
   IMPORTANT: Use proper Spanish characters with accents (sesion -> sesion with accent, contrasena -> contrasena with tilde, etc.) — match the encoding used in the existing es.json file.
  </action>
  <verify>
    <automated>cd C:/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>loginSchema exports correctly, en.json and es.json both contain "login" namespace with all required keys, TypeScript compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Build LoginForm component and replace login page</name>
  <files>
    web/src/components/auth/login-form.tsx
    web/src/app/(auth)/login/page.tsx
  </files>
  <action>
1. Create `web/src/components/auth/login-form.tsx` as a `"use client"` component following the SignupForm pattern exactly:
   - Import: `useForm` from react-hook-form, `zodResolver` from @hookform/resolvers/zod, `useTranslations` from next-intl, `Eye`/`EyeOff`/`Loader2` from lucide-react
   - Import: `loginSchema, type LoginFormData` from `@/lib/validations/login`
   - Import: `Button`, `Input`, `Label` from UI components
   - Import: `GoogleOAuthButton` from `@/components/auth/google-oauth-button`
   - Import: `Link` from next/link, `useRouter` from next/navigation, `useState` from react

   Form structure (top to bottom):
   a. Root error display (same red banner pattern as SignupForm `errors.root`)
   b. Email field with label, placeholder from t("emailPlaceholder"), autoComplete="email"
   c. Password field with label, show/hide toggle (Eye/EyeOff icons — copy exact pattern from SignupForm), autoComplete="current-password"
   d. Row below password: "Remember me" checkbox on left (`<input type="checkbox" {...register("rememberMe")} />`), "Forgot password?" link on right (`<Link href="/forgot-password">` — right-aligned with `ml-auto`)
   e. Submit button with Loader2 spinner when isSubmitting
   f. Divider line with "or" text (same pattern as SignupForm)
   g. GoogleOAuthButton with `redirectTo="/dashboard"`
   h. "Don't have an account? Sign up" link to /signup (NOTE: CONTEXT.md says "/register" but Phase 4 established /signup as the canonical route — using /signup per existing codebase)

   Form behavior:
   - `useForm<LoginFormData>` with `zodResolver(loginSchema)`, `mode: "onBlur"`, `reValidateMode: "onChange"`, `defaultValues: { rememberMe: false }`
   - `handleFormSubmit` is a placeholder for now — it does NOT call any Server Action yet (Plan 02 wires that). For now, just `console.log("Login submit:", data)` so the form is testable.
   - The form renders `errors.root?.message` using the translation key lookup pattern: `t(\`errors.${errors.root.message}\`)` if the message matches a known key, otherwise show the raw message. Plan 02 will set these error keys.
   - Password field cleared on error: `setValue("password", "")` — prepare this in the error handler stub.

   Rate limiting UI (prepare state, Plan 02 activates):
   - `const [failedAttempts, setFailedAttempts] = useState(0)`
   - `const [lockedUntil, setLockedUntil] = useState<number | null>(null)`
   - If `lockedUntil && Date.now() < lockedUntil`, disable the submit button and show the "tooManyAttempts" message with countdown minutes. Use `useEffect` with a 1-second interval to update remaining time.
   - Disable submit button when `isSubmitting || isLocked`

   Styling: Match SignupForm exactly — same spacing (space-y-4), same Label/Input sizing, same button styling (w-full), same muted-foreground colors for links.

2. Replace `web/src/app/(auth)/login/page.tsx` entirely. Convert from "use client" to an async Server Component matching the signup page pattern:
   - Import `getTranslations` from "next-intl/server"
   - Import `LoginForm` from "@/components/auth/login-form"
   - Import `LanguageSwitcher` from "@/components/auth/language-switcher"
   - Import `Image` from "next/image"
   - Import `CheckCircle, TrendingUp, Users` from "lucide-react" (different icons than signup to distinguish the page visually)

   Left panel (branding — Claude's discretion per CONTEXT.md):
   - Same dark background (#111), same grid pattern overlay, same layout structure as signup
   - Different content: "Welcome back" headline, "Your automations are running" subheadline
   - Three highlights with CheckCircle, TrendingUp, Users icons using t("branding.highlight1/2/3")
   - Same logo at top-left (Image src="/logo.png" with brightness-0 invert)

   Right panel:
   - LanguageSwitcher absolute top-right (same as signup)
   - Mobile-only logo (same as signup)
   - Form header: t("title") + t("subtitle")
   - `<LoginForm />` — no wrapper needed (no reCAPTCHA for login)

   Read searchParams for `?reason=expired` and `?error=auth`:
   - If `reason=expired`, show a dismissible info banner above the form: t("sessionExpired") in a blue/info style
   - If `error=auth`, show a dismissible error banner: t("errors.authError") — per CONTEXT.md, Google OAuth errors disappear in 5 seconds
   - Pass these as props to LoginForm: `sessionExpired?: boolean`, `authError?: boolean`
   - LoginForm shows these as dismissible banners at the top of the form
   - **Auto-dismiss after 5 seconds:** Add state `const [showAuthError, setShowAuthError] = useState(authError)` and `const [showSessionExpired, setShowSessionExpired] = useState(sessionExpired)`. Add useEffect:
     ```typescript
     useEffect(() => {
       if (showAuthError) {
         const timer = setTimeout(() => setShowAuthError(false), 5000)
         return () => clearTimeout(timer)
       }
     }, [showAuthError])
     useEffect(() => {
       if (showSessionExpired) {
         const timer = setTimeout(() => setShowSessionExpired(false), 5000)
         return () => clearTimeout(timer)
       }
     }, [showSessionExpired])
     ```
   - Render banners conditionally on `showAuthError` / `showSessionExpired` state (not the raw props) so they disappear after 5 seconds
  </action>
  <verify>
    <automated>cd C:/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Login page renders as Server Component with split layout, LoginForm client component has all form fields (email, password with toggle, remember me, forgot password link, Google OAuth, signup link), i18n works for all text, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `cd web && npx tsc --noEmit` — zero TypeScript errors
2. `cd web && npm run build 2>&1 | tail -20` — build succeeds, /login page compiles
3. Visual: /login page shows split layout (dark left panel with welcome-back branding, form on right)
4. Visual: Form has email, password (with eye toggle), remember me checkbox, forgot password link, sign in button, Google OAuth button, signup link
5. Visual: Language switcher in top-right toggles between EN/ES, all text updates
6. Visual: On mobile viewport, left panel hidden, form centered with logo above
</verification>

<success_criteria>
- Login page at /login renders with split layout matching signup design
- All form fields present: email, password (show/hide), remember me, forgot password link
- Google OAuth button visible below form with divider
- "Don't have an account? Sign up" link visible
- Language switcher works, all text translatable EN/ES
- Zod validation fires on blur for email/password
- TypeScript compiles with zero errors
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-login/05-01-SUMMARY.md`
</output>
