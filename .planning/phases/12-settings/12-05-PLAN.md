---
phase: 12-settings
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/dashboard/settings-security-card.tsx
autonomous: true
gap_closure: true
requirements: [SETT-06]

must_haves:
  truths:
    - "Settings page loads without hydration errors in the browser console"
    - "Security card shows correct browser name, OS, and timezone on the client"
  artifacts:
    - path: "web/src/components/dashboard/settings-security-card.tsx"
      provides: "Client-only device detection without SSR hydration mismatch"
      contains: "useEffect"
  key_links:
    - from: "web/src/components/dashboard/settings-security-card.tsx"
      to: "useState/useEffect"
      via: "deferred client-side detection"
      pattern: "useEffect.*detect"
---

<objective>
Fix hydration mismatch in the Security card's session display.

Purpose: The detectBrowser/detectOS/timezone detection runs during SSR where navigator is undefined, producing "Unknown browser" on the server but the real value on the client, causing a React hydration error.
Output: Session info deferred to client-side via useState + useEffect, eliminating the hydration mismatch.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-settings/12-UAT.md

<interfaces>
<!-- From web/src/components/dashboard/settings-security-card.tsx: -->
<!-- Lines 146-148 (the problem): -->
<!-- const browser = detectBrowser()   // returns "Unknown" on server, "Microsoft Edge" on client -->
<!-- const os = detectOS()             // returns "" on server, "Windows" on client -->
<!-- const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone  // differs server vs client -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Defer device detection to client-side with useState + useEffect</name>
  <files>web/src/components/dashboard/settings-security-card.tsx</files>
  <action>
  Open `web/src/components/dashboard/settings-security-card.tsx` and fix the hydration mismatch:

  1. Remove lines 146-150 (the direct calls to detectBrowser/detectOS/timezone in the component body):
     ```
     // DELETE these lines:
     const browser = detectBrowser()
     const os = detectOS()
     const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone
     const deviceLabel = [browser, os].filter(Boolean).join(' on ')
     ```

  2. Replace with useState + useEffect pattern. Add a new state near the top of the component (after the existing useState declarations around line 79-80):
     ```
     const [deviceInfo, setDeviceInfo] = useState<{
       browser: string
       os: string
       timezone: string
     } | null>(null)
     ```

  3. Add a useEffect (after the existing toast useEffect around line 83-87):
     ```
     useEffect(() => {
       setDeviceInfo({
         browser: detectBrowser(),
         os: detectOS(),
         timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
       })
     }, [])
     ```

  4. Compute deviceLabel from the state:
     ```
     const deviceLabel = deviceInfo
       ? [deviceInfo.browser, deviceInfo.os].filter(Boolean).join(' on ')
       : ''
     const timezone = deviceInfo?.timezone ?? ''
     ```

  5. The render section already uses `deviceLabel` and `timezone` — no changes needed there. The initial SSR render will show empty strings (matching on both server and client), then the useEffect fills in the real values on the client. This eliminates the hydration mismatch.

  Do NOT change the detectBrowser() or detectOS() helper functions themselves — they are fine. The issue is only WHERE they are called (component body vs useEffect).
  </action>
  <verify>
    <automated>cd c:/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Security card renders without hydration errors. On initial load, session info briefly shows empty then fills with browser/OS/timezone. No "Unknown browser" vs "Microsoft Edge" mismatch in console.</done>
</task>

</tasks>

<verification>
1. `cd c:/dev/12ai/web && npm run build` — builds without errors
2. Manual: Navigate to /dashboard/settings — no hydration error in browser console
3. Manual: Security card shows correct browser name, OS, and timezone
</verification>

<success_criteria>
- UAT test 1 (hydration error) is resolved
- No React hydration mismatch warnings in the browser console
- Session info displays correctly after client-side hydration
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings/12-05-SUMMARY.md`
</output>
