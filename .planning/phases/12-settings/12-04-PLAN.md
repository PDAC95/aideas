---
phase: 12-settings
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/actions/settings.ts
  - web/src/components/dashboard/dashboard-header.tsx
autonomous: true
gap_closure: true
requirements: [SETT-01, SETT-02, SETT-04]

must_haves:
  truths:
    - "Avatar uploads persist after page refresh"
    - "Remove avatar button clears avatar and reverts to initials"
    - "Saved name updates the header user menu immediately"
    - "Company name persists after page refresh"
    - "Hourly cost persists after page refresh and shows success toast"
  artifacts:
    - path: "web/src/lib/actions/settings.ts"
      provides: "Server actions with revalidatePath and auth metadata sync"
      contains: "revalidatePath"
    - path: "web/src/components/dashboard/dashboard-header.tsx"
      provides: "Header reading name from profiles table instead of auth metadata"
      contains: "profiles"
  key_links:
    - from: "web/src/lib/actions/settings.ts"
      to: "next/cache"
      via: "revalidatePath import"
      pattern: "revalidatePath"
    - from: "web/src/lib/actions/settings.ts"
      to: "supabase.auth.updateUser"
      via: "auth metadata sync in saveProfileName"
      pattern: "auth\\.updateUser"
---

<objective>
Fix 5 UAT failures caused by missing revalidatePath calls in server actions and stale header data.

Purpose: All settings save actions (avatar, name, company name, hourly cost) must persist after page refresh, and the header must reflect name changes immediately.
Output: Corrected server actions with cache invalidation and header reading from profiles table.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-settings/12-UAT.md

<interfaces>
<!-- From web/src/lib/actions/settings.ts (current state): -->
export async function saveProfileName(firstName: string, lastName: string): Promise<{ success: true } | { error: string }>
export async function saveCompanyName(orgId: string, companyName: string): Promise<{ success: true } | { error: string }>
export async function saveAvatarUrl(avatarUrl: string | null): Promise<{ success: true } | { error: string }>
export async function saveHourlyCost(orgId: string, hourlyCost: number): Promise<{ success: true } | { error: string }>

<!-- From web/src/components/dashboard/dashboard-header.tsx: -->
<!-- Line 22: reads user.user_metadata?.first_name — this is the stale source -->
<!-- Line 23: const displayName = firstName || user.email?.split("@")[0] || "User" -->
<!-- Line 24: const avatarInitial = (firstName || user.email || "U").charAt(0).toUpperCase() -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add revalidatePath and auth metadata sync to all server actions</name>
  <files>web/src/lib/actions/settings.ts</files>
  <action>
  Open `web/src/lib/actions/settings.ts` and apply these changes:

  1. Add import at top: `import { revalidatePath } from 'next/cache'`

  2. In `saveProfileName` (line 27-48):
     - After the successful `.update()` on profiles table (line 41-44), add auth metadata sync:
       ```
       await supabase.auth.updateUser({
         data: { first_name: fn, last_name: ln, full_name: fullName }
       })
       ```
     - Before `return { success: true }`, add: `revalidatePath('/dashboard', 'layout')`
     - This ensures the header (rendered in the dashboard layout) picks up the new name from auth metadata AND the profiles table is updated.

  3. In `saveCompanyName` (line 54-85):
     - Before `return { success: true }` (line 84), add: `revalidatePath('/dashboard/settings')`

  4. In `saveAvatarUrl` (line 91-105):
     - Before `return { success: true }` (line 104), add: `revalidatePath('/dashboard', 'layout')`
     - The avatar may display in the header/layout, so invalidate the layout.

  5. In `saveHourlyCost` (line 115-159):
     - Before `return { success: true }` (line 158), add: `revalidatePath('/dashboard/settings')`

  All revalidatePath calls go AFTER the successful DB write and BEFORE the return statement. Do NOT add revalidatePath on error paths.
  </action>
  <verify>
    <automated>cd c:/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>All 4 server actions (saveProfileName, saveCompanyName, saveAvatarUrl, saveHourlyCost) call revalidatePath after successful writes. saveProfileName also syncs auth user_metadata. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix header to read name from profiles table via query</name>
  <files>web/src/components/dashboard/dashboard-header.tsx</files>
  <action>
  The dashboard header currently reads the user's name from `user.user_metadata?.first_name` (line 22). After Task 1 syncs auth metadata, this will work for NEW saves. However, to be robust and consistent with the profiles table as source of truth:

  1. Open `web/src/components/dashboard/dashboard-header.tsx`.
  2. Add import: `import { createClient } from '@/lib/supabase/server'`
  3. Inside `DashboardHeader` (it's already an async server component), after `const tNotif = ...` (line 20), add a query to fetch the profile name:
     ```
     const supabase = await createClient()
     const { data: profile } = await supabase
       .from('profiles')
       .select('first_name, avatar_url')
       .eq('id', user.id)
       .single()
     ```
  4. Change line 22-24 to use profile data with fallback to user_metadata:
     ```
     const firstName = profile?.first_name || (user.user_metadata?.first_name as string) || ""
     const displayName = firstName || user.email?.split("@")[0] || "User"
     const avatarInitial = (firstName || user.email || "U").charAt(0).toUpperCase()
     ```

  This ensures the header always reflects the latest profiles table data, not just stale auth metadata.
  </action>
  <verify>
    <automated>cd c:/dev/12ai/web && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard header reads displayName from profiles table (with auth metadata fallback). After saving name in settings, refreshing any dashboard page shows the updated name in the header user menu.</done>
</task>

</tasks>

<verification>
1. `cd c:/dev/12ai/web && npm run build` — builds without errors
2. Manual: Save profile name in settings, refresh — name persists AND header updates
3. Manual: Upload avatar, refresh — avatar persists
4. Manual: Change company name, refresh — company name persists
5. Manual: Set hourly cost, save — toast appears, refresh — value persists
</verification>

<success_criteria>
- All 5 UAT gaps related to data persistence (tests 2, 3, 4, 5, 7) are resolved
- Server actions invalidate Next.js cache after successful writes
- Header displays fresh profile data from the database
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings/12-04-SUMMARY.md`
</output>
