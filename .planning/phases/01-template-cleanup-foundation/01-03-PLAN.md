---
phase: 01-template-cleanup-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
autonomous: false
files_modified:
  - landing/index.html
  - landing/automations.html
  - landing/pricing.html
  - landing/contact.html
  - landing/404.html
requirements:
  - FOUN-06
  - NAVL-05
  - PERF-02

must_haves:
  truths:
    - "index.html, automations.html, pricing.html, contact.html all exist in landing/"
    - "All 4 pages navigate between each other via SWUP without white flash or JS errors"
    - "All 4 pages show AIDEAS navigation with correct active state per page"
    - "All 4 pages show the AIDEAS footer (no Ashley/agency content)"
    - "Hamburger menu opens on mobile (<992px) on all 4 pages and shows AIDEAS nav links"
    - "All unused template pages (blog, portfolio, project, team, service, home-2) are deleted"
    - "404.html has AIDEAS branding"
  artifacts:
    - path: "landing/index.html"
      provides: "Renamed from home-1.html, nav active on Home"
      contains: "class=\"mil-active\"><a href=\"index.html\""
    - path: "landing/automations.html"
      provides: "New page with AIDEAS shell, placeholder content, nav active on Automations"
      contains: "class=\"mil-active\"><a href=\"automations.html\""
    - path: "landing/pricing.html"
      provides: "New page with AIDEAS shell, placeholder content, nav active on Pricing"
      contains: "class=\"mil-active\"><a href=\"pricing.html\""
    - path: "landing/contact.html"
      provides: "Adapted from template contact.html, nav active on Contact"
      contains: "class=\"mil-active\"><a href=\"contact.html\""
  key_links:
    - from: "all 4 pages .mil-main-menu#swupMenu"
      to: "each other via SWUP"
      via: "flat <ul> with correct active state per page"
      pattern: "swupMenu.*mil-active"
    - from: "all 4 pages #swupMain"
      to: ".mil-hidden-elements block"
      via: "must exist on each page for GSAP clone logic"
      pattern: "mil-hidden-elements"
---

<objective>
Create the full set of 4 HTML pages from the cleaned `home-1.html` template, delete all unused template pages, and verify SWUP navigation works across all pages.

Purpose: Plan 02 cleaned `home-1.html` into the master template. This plan multiplies it into 4 pages with correct per-page nav active states, placeholder content, and then deletes all the agency template pages that would pollute the project.

Output: 4 clean HTML pages with working SWUP navigation, correct per-page active states, and no unused template files remaining.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-template-cleanup-foundation/01-RESEARCH.md
@.planning/phases/01-template-cleanup-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Shell structure to copy from index.html (cleaned home-1.html) -->
<!-- Each new page copies everything EXCEPT the #swupMain content sections -->
<!-- Per-page active state: set mil-active on the correct <li> in id="swupMenu" -->

Nav structure (from plan 02 output):
  nav.mil-main-menu#swupMenu ul:
    li.mil-active > a href="index.html"    (active on index.html only)
    li > a href="automations.html"
    li > a href="pricing.html"
    li > a href="contact.html"

SWUP replaces #swupMenu on navigation — each page's swupMenu must set its own active state in HTML.

.mil-hidden-elements block (must appear at end of each #swupMain):
  Contains dodecahedron div, arrow SVG, lines SVG.
  Required: main.js clones from this on every page load and navigation.

Files to DELETE (confirmed from research):
  blog.html, blog-inner.html, publication.html
  portfolio-1.html, portfolio-2.html, portfolio-3.html
  project-1.html through project-6.html
  service.html, services.html, team.html, home-2.html
  home-1.html (after rename to index.html)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename home-1.html to index.html and create automations.html + pricing.html</name>
  <files>
    landing/index.html
    landing/automations.html
    landing/pricing.html
  </files>
  <action>
**Step 1: Rename home-1.html to index.html**

Copy `landing/home-1.html` to `landing/index.html`. Then delete `landing/home-1.html`.

```bash
cp /c/dev/12ai/landing/home-1.html /c/dev/12ai/landing/index.html
rm /c/dev/12ai/landing/home-1.html
```

The `index.html` already has the Home `<li>` marked as `mil-active` (from plan 02). Verify the active state:
```html
<li class="mil-active"><a href="index.html">Home</a></li>
```

**Step 2: Create automations.html**

Copy `landing/index.html` to `landing/automations.html`, then make these targeted changes to `automations.html`:

1. Change `<title>` to: `<title>Automations — AIDEAS AI Automation Catalog</title>`

2. Update `id="swupMenu"` nav — move `mil-active` to Automations:
```html
<nav class="mil-main-menu" id="swupMenu">
    <ul>
        <li><a href="index.html">Home</a></li>
        <li class="mil-active"><a href="automations.html">Automations</a></li>
        <li><a href="pricing.html">Pricing</a></li>
        <li><a href="contact.html">Contact</a></li>
    </ul>
</nav>
```

3. Replace the `#swupMain` content section (everything between the opening `<div id="swupMain" class="mil-main-transition">` and the `.mil-hidden-elements` div) with a placeholder banner:
```html
<!-- banner -->
<section class="mil-dark-bg mil-p-120-60">
    <div class="container">
        <div class="mil-up">
            <h1 class="mil-muted mil-mb-30">Automations</h1>
            <p class="mil-light-soft">Explore 150+ AI automations. Coming in Phase 4.</p>
        </div>
    </div>
</section>
<!-- banner end -->
```

Preserve the footer and `.mil-hidden-elements` block exactly as in `index.html`.

**Step 3: Create pricing.html**

Copy `landing/index.html` to `landing/pricing.html`, then make these targeted changes:

1. Change `<title>` to: `<title>Pricing — AIDEAS Automation Plans</title>`

2. Update nav active state to Pricing:
```html
<nav class="mil-main-menu" id="swupMenu">
    <ul>
        <li><a href="index.html">Home</a></li>
        <li><a href="automations.html">Automations</a></li>
        <li class="mil-active"><a href="pricing.html">Pricing</a></li>
        <li><a href="contact.html">Contact</a></li>
    </ul>
</nav>
```

3. Replace `#swupMain` content section:
```html
<!-- banner -->
<section class="mil-dark-bg mil-p-120-60">
    <div class="container">
        <div class="mil-up">
            <h1 class="mil-muted mil-mb-30">Pricing</h1>
            <p class="mil-light-soft">Simple, transparent pricing for every team. Coming in Phase 5.</p>
        </div>
    </div>
</section>
<!-- banner end -->
```

Preserve footer and `.mil-hidden-elements` block.
  </action>
  <verify>
    <automated>ls /c/dev/12ai/landing/index.html /c/dev/12ai/landing/automations.html /c/dev/12ai/landing/pricing.html && grep -l "mil-active" /c/dev/12ai/landing/index.html /c/dev/12ai/landing/automations.html /c/dev/12ai/landing/pricing.html</automated>
  </verify>
  <done>
All 3 files exist. Each has `mil-active` on its own nav item. `home-1.html` no longer exists. Titles are distinct per page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt contact.html, update 404.html, delete unused pages</name>
  <files>
    landing/contact.html
    landing/404.html
  </files>
  <action>
**Part A: Adapt contact.html**

Read the existing `landing/contact.html`. Replace its entire shell (everything outside and around `#swupMain`) with the AIDEAS shell from `index.html`. Strategy:

1. Replace `<head>` content with the head from `index.html`, changing only the title:
   `<title>Contact — Request an AIDEAS Demo</title>`

2. Replace all elements OUTSIDE `#swupMain` with AIDEAS versions from `index.html`:
   - `.mil-ball` — copy from index.html
   - `.mil-preloader` — copy from index.html ("Automate / Save Time / Scale")
   - `.mil-progress-track` — copy from index.html
   - `.mil-menu-frame` — copy from index.html, but update nav active state to Contact:
     ```html
     <nav class="mil-main-menu" id="swupMenu">
         <ul>
             <li><a href="index.html">Home</a></li>
             <li><a href="automations.html">Automations</a></li>
             <li><a href="pricing.html">Pricing</a></li>
             <li class="mil-active"><a href="contact.html">Contact</a></li>
         </ul>
     </nav>
     ```
   - `.mil-curtain` — copy from index.html
   - `.mil-frame` — copy from index.html (logo = AIDEAS, link = index.html)

3. Replace `#swupMain` content with placeholder banner + footer + hidden elements:
```html
<div id="swupMain" class="mil-main-transition">

    <!-- banner -->
    <section class="mil-dark-bg mil-p-120-60">
        <div class="container">
            <div class="mil-up">
                <h1 class="mil-muted mil-mb-30">Contact Us</h1>
                <p class="mil-light-soft">Request a demo or ask us anything. Coming in Phase 6.</p>
            </div>
        </div>
    </section>
    <!-- banner end -->

    <!-- Phase 6: Full contact form will be built here -->

    [COPY FOOTER EXACTLY FROM index.html]

    [COPY .mil-hidden-elements EXACTLY FROM index.html]

</div>
```

4. Replace script tags at bottom with AIDEAS deferred scripts (same 9 scripts as index.html).

**Part B: Update 404.html**

Read `landing/404.html`. Apply the same shell replacement strategy:
- Title: `<title>Page Not Found — AIDEAS</title>`
- AIDEAS preloader (same "Automate / Save Time / Scale")
- Nav: no active state (all items without mil-active class)
- AIDEAS footer
- Same deferred scripts

Keep whatever 404-specific content exists inside `#swupMain` but remove any Ashley/agency text references.

**Part C: Delete all unused template pages**

```bash
rm /c/dev/12ai/landing/blog.html
rm /c/dev/12ai/landing/blog-inner.html
rm /c/dev/12ai/landing/publication.html
rm /c/dev/12ai/landing/portfolio-1.html
rm /c/dev/12ai/landing/portfolio-2.html
rm /c/dev/12ai/landing/portfolio-3.html
rm /c/dev/12ai/landing/project-1.html
rm /c/dev/12ai/landing/project-2.html
rm /c/dev/12ai/landing/project-3.html
rm /c/dev/12ai/landing/project-4.html
rm /c/dev/12ai/landing/project-5.html
rm /c/dev/12ai/landing/project-6.html
rm /c/dev/12ai/landing/service.html
rm /c/dev/12ai/landing/services.html
rm /c/dev/12ai/landing/team.html
rm /c/dev/12ai/landing/home-2.html
```
  </action>
  <verify>
    <automated>ls /c/dev/12ai/landing/*.html && echo "---" && grep -l "zxx\|Ashley\|Pioneering\|portfolio-\|home-1" /c/dev/12ai/landing/*.html 2>/dev/null || echo "No template placeholders found"</automated>
  </verify>
  <done>
Only index.html, automations.html, pricing.html, contact.html, 404.html exist in landing/. No HTML files contain "zxx", "Ashley", "Pioneering", or template portfolio links. All deleted pages no longer exist.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification — SWUP navigation and mobile hamburger</name>
  <action>
Run `cd /c/dev/12ai/landing && npm run dev` to start dev server, then verify all 8 checks listed in how-to-verify below.
  </action>
  <verify>
    <automated>ls /c/dev/12ai/landing/*.html | wc -l</automated>
  </verify>
  <done>All 8 visual and functional checks pass as confirmed by the user.</done>
  <what-built>
4 HTML pages with AIDEAS shell (preloader, nav, footer), deferred scripts, SWUP navigation between them, and hamburger mobile menu. All unused template pages deleted.
  </what-built>
  <how-to-verify>
Run `cd /c/dev/12ai/landing && npm run dev` (or `npx live-server --port=3000` if npm dev has issues) and open http://localhost:3000.

Check all 8 items:

1. Preloader: Hard-reload the page. Should show "Automate / Save Time / Scale" animation, then "aideas.com" reveal. Should NOT show "Pioneering Creative Excellence".

2. Navigation (desktop): 4 nav links visible — Home, Automations, Pricing, Contact. No dropdown arrows.

3. SWUP transitions: Click Automations — smooth fade transition — Automations page loads with placeholder text. Click Pricing, Contact, Home in sequence. No white flash, no full page reload between pages.

4. Active state: Each page highlights its own nav item (Home link highlighted on index, Automations highlighted on automations page, etc.).

5. Footer: Scroll to bottom of any page. Shows AIDEAS branding, nav links, social icons, "2024 AIDEAS. All Rights Reserved." No Ashley agency content.

6. Hamburger (mobile): DevTools device toolbar at less than 992px width. Click hamburger icon — full overlay opens with AIDEAS links. Click a link — overlay closes and SWUP navigates.

7. External CTA: Click "Get Started Free" button — browser navigates to app.aideas.com/signup. SWUP does NOT intercept (no smooth transition, just normal navigation or new tab).

8. Console errors: DevTools Console open while navigating between all pages. Zero uncaught JavaScript errors.

Type "approved" when all 8 pass, or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
```bash
# All 4 target pages exist
ls /c/dev/12ai/landing/*.html

# No template placeholders in any page
grep -in "zxx\|ashley\|pioneering\|creative excellence\|home-1\|portfolio-1" /c/dev/12ai/landing/*.html

# Each page has correct active state
grep -n "mil-active" /c/dev/12ai/landing/index.html
grep -n "mil-active" /c/dev/12ai/landing/automations.html
grep -n "mil-active" /c/dev/12ai/landing/pricing.html
grep -n "mil-active" /c/dev/12ai/landing/contact.html

# .mil-hidden-elements present on all pages
grep -l "mil-hidden-elements" /c/dev/12ai/landing/*.html

# defer on all pages
grep -c "defer" /c/dev/12ai/landing/index.html
```
</verification>

<success_criteria>
- Exactly 5 HTML files in landing/: index.html, automations.html, pricing.html, contact.html, 404.html
- All 16 unused template pages deleted
- All 4 pages have AIDEAS nav with correct per-page active state
- All 4 pages have AIDEAS footer
- All 4 pages have .mil-hidden-elements block inside #swupMain
- All 4 pages have defer on all scripts
- SWUP transitions work between all 4 pages (verified by human in checkpoint)
- Hamburger menu works on mobile (verified by human in checkpoint)
- No console errors during navigation (verified by human in checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-template-cleanup-foundation/01-03-SUMMARY.md` using the summary template.
</output>
