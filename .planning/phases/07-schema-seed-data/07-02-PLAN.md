---
phase: 07-schema-seed-data
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - supabase/seed.sql
  - web/messages/en.json
  - web/messages/es.json
autonomous: true
requirements: [DATA-04]
must_haves:
  truths:
    - "seed.sql contains 66+ automation template INSERT rows with i18n keys for name and description"
    - "Each template has setup_price (9900-49900), monthly_price (2900-14900), setup_time_days (1-5), industry_tags, connected_apps, avg_minutes_per_task (5-45), activity_metric_label"
    - "Templates span 8 categories (customer_service, documents, marketing, sales, operations, productivity, reports, ai_agents) with ~8 per category"
    - "Templates tagged across 6 industries (retail, salud, legal, inmobiliaria, restaurantes, agencias)"
    - "en.json and es.json contain matching translation entries for all 66+ template names, descriptions, impact text, and metric labels"
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Complete expanded seed with 66+ templates"
      contains: "automation_templates"
    - path: "web/messages/en.json"
      provides: "English translations for template catalog"
      contains: "templates"
    - path: "web/messages/es.json"
      provides: "Spanish translations for template catalog"
      contains: "templates"
  key_links:
    - from: "supabase/seed.sql"
      to: "web/messages/en.json"
      via: "i18n key references — DB stores keys like templates.ai_chatbot_24_7.name, message files have actual text"
      pattern: "templates\\."
---

<objective>
Replace the seed.sql template section with 66+ automation templates using i18n keys, and add corresponding EN/ES translations to the message files.

Purpose: The automation catalog (Phase 10) and dashboard home (Phase 8) need a rich set of realistic templates with proper bilingual support. This is the data foundation.
Output: Updated seed.sql with 66+ templates, updated en.json and es.json with template translations
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-schema-seed-data/07-CONTEXT.md
@.planning/phases/07-schema-seed-data/07-RESEARCH.md
@.planning/phases/07-schema-seed-data/07-01-SUMMARY.md

<interfaces>
<!-- Existing seed.sql structure — full file at supabase/seed.sql (625 lines) -->
<!-- The seed uses TRUNCATE + INSERT with fixed UUIDs, wrapped in BEGIN/COMMIT -->
<!-- Existing UUID conventions: -->
<!--   org: aaaaaaaa-0000-0000-0000-000000000001 (Acme Corp) -->
<!--   org: bbbbbbbb-0000-0000-0000-000000000001 (GlobalTech) -->
<!--   users: a1111111-..., a2222222-..., b1111111-..., b2222222-..., de000000-... -->
<!--   templates: tt000001-0000-0000-0000-00000000000N (old convention) -->
<!-- New template UUID convention: tt{category_num}{seq}-0000-0000-0000-000000000001 -->

<!-- en.json structure (existing) — flat object with nested keys -->
<!-- Top-level keys: signup, login, resetPassword, landing -->
<!-- Template keys go under new "templates" top-level key -->

<!-- automation_templates columns after migration: -->
<!-- id, name, slug, description, category, icon, -->
<!-- setup_price, monthly_price, setup_time_days, -->
<!-- industry_tags, connected_apps, -->
<!-- typical_impact_text, avg_minutes_per_task, activity_metric_label, -->
<!-- features, use_cases, config_schema, pricing_tier, is_active, is_featured, sort_order -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite seed.sql with 66+ automation templates</name>
  <files>supabase/seed.sql</files>
  <action>
Replace the entire `supabase/seed.sql` file. The new seed must:

**Structure (preserve existing pattern):**
1. BEGIN transaction
2. Disable on_auth_user_created trigger
3. TRUNCATE all tables in reverse FK order (use CASCADE): invitations, notifications, chat_messages, subscriptions, automation_requests, automation_executions, automations, automation_templates, organization_members, profiles, organizations
4. DELETE FROM auth.identities; DELETE FROM auth.users;
5. Re-enable trigger
6. INSERT auth.users (keep existing 5 users exactly as-is)
7. INSERT auth.identities (keep existing 5 exactly as-is)
8. INSERT organizations (keep existing 2 exactly as-is)
9. INSERT profiles (keep existing 5 exactly as-is)
10. INSERT organization_members (keep existing 5 exactly as-is)
11. **INSERT automation_templates (66+ rows — NEW)**
12. Placeholder comment for automations (Plan 03 will add)
13. Placeholder comment for executions (Plan 03 will add)
14. Placeholder comment for requests (Plan 03 will add)
15. INSERT subscriptions (keep existing 2 exactly as-is)
16. INSERT chat_messages (keep existing 5 exactly as-is)
17. Placeholder comment for notifications (Plan 03 will expand)
18. INSERT invitations (keep existing 1 exactly as-is)
19. COMMIT

**CRITICAL CHANGE: TRUNCATE instead of ON CONFLICT** — per CONTEXT.md decision, use TRUNCATE CASCADE at the top instead of ON CONFLICT DO NOTHING on each INSERT. This makes seed idempotent via clean slate, not conflict handling. Remove all `ON CONFLICT` clauses from INSERT statements.

**Template catalog requirements (66+ templates):**

Distribute ~8 templates per category across 8 categories:
- `sales` (~8): Lead follow-up, CRM sync, proposal generation, quote builder, pipeline alerts, territory reports, win/loss analysis, sales forecasting
- `marketing` (~8): Content generation, social scheduling, lead scoring, email campaigns, SEO monitoring, ad performance, audience segmentation, newsletter automation
- `customer_service` (~9): AI chatbot, auto-response email, ticket routing, satisfaction surveys, SLA monitoring, knowledge base updater, escalation manager, FAQ bot, review response
- `documents` (~8): Invoice processing, report generation, contract analysis, data extraction, document approval, template filling, receipt scanning, compliance checker
- `operations` (~8): Data reconciliation, inventory sync, shift scheduling, vendor management, quality monitoring, workflow orchestrator, system health monitor, backup verification
- `productivity` (~8): Meeting notes AI, task assignment, time tracking sync, calendar optimizer, Slack digest, project status reports, daily standup bot, resource planner
- `reports` (~8): Executive dashboard, KPI tracker, financial summary, client performance, team productivity, marketing ROI, operations scorecard, custom analytics
- `ai_agents` (~9): Multi-channel support agent, research assistant, data analyst agent, content strategist agent, sales copilot, HR onboarding agent, code review agent, compliance agent, competitive intelligence agent

For each template row:
- `id`: Fixed UUID pattern `tt{cat}{seq}-0000-0000-0000-000000000001` where cat=01-08, seq=01-09
  - cat 01 = sales, 02 = marketing, 03 = customer_service, 04 = documents, 05 = operations, 06 = productivity, 07 = reports, 08 = ai_agents
- `name`: i18n key like `templates.lead_followup_email.name`
- `slug`: kebab-case like `lead-followup-email`
- `description`: i18n key like `templates.lead_followup_email.description`
- `category`: one of the 8 valid categories
- `icon`: Lucide icon name (message-circle, mail, file-text, bar-chart-2, target, user-plus, git-merge, zap, brain, calendar, etc.)
- `setup_price`: INTEGER cents between 9900-49900 (complexity-based: simple=9900, medium=19900, complex=29900-49900)
- `monthly_price`: INTEGER cents between 2900-14900 (simple=2900-4900, medium=4900-7900, complex=7900-14900)
- `setup_time_days`: 1-5 (correlates with setup_price)
- `industry_tags`: TEXT[] with 1-3 industries from: 'retail', 'salud', 'legal', 'inmobiliaria', 'restaurantes', 'agencias'. Some templates are cross-industry (3 tags), some are niche (1 tag). Ensure each industry appears in 15-25 templates.
- `connected_apps`: TEXT[] with 2-4 apps from: 'Google Workspace', 'Slack', 'HubSpot', 'Salesforce', 'QuickBooks', 'Shopify', 'Mailchimp', 'Notion', 'Stripe', 'Zendesk', 'Calendly', 'Zoom'. Pick apps that make sense for the automation type.
- `typical_impact_text`: i18n key like `templates.lead_followup_email.impact`
- `avg_minutes_per_task`: 5-45 (simple email=5-10, complex reports=30-45)
- `activity_metric_label`: i18n key like `templates.lead_followup_email.metric_label`
- `features`: TEXT[] with 3-4 feature strings (can be English — these are technical, not displayed to end users)
- `use_cases`: TEXT[] with 2-3 use case strings (same — technical)
- `config_schema`: '{}'::jsonb
- `pricing_tier`: 'starter' for simple, 'pro' for medium, 'business' for complex
- `is_active`: true for all
- `is_featured`: true for ~10-12 standout templates (distribute across categories)
- `sort_order`: sequential within category (1-9)

**Key convention for i18n keys:** Use snake_case slug derived from template name:
- Template "Lead Follow-up Email Sequence" -> key slug `lead_followup_email`
- DB stores: `templates.lead_followup_email.name`, `templates.lead_followup_email.description`, etc.
- 4 keys per template: `.name`, `.description`, `.impact`, `.metric_label`

Use the snake_case slug as the base, NOT the kebab-case slug. Keep them distinct:
- slug column (kebab): `lead-followup-email` (for URLs)
- i18n key slug (snake): `lead_followup_email` (for translation lookup)
  </action>
  <verify>
    <automated>cd C:/dev/12ai && grep -c "INSERT INTO public.automation_templates" supabase/seed.sql && grep -c "tt0" supabase/seed.sql</automated>
  </verify>
  <done>seed.sql contains 66+ template rows with i18n keys for name/description/impact/metric_label. All 8 categories represented with ~8 templates each. Each template has realistic pricing, industry tags, connected apps, and metrics. TRUNCATE CASCADE at top, no ON CONFLICT clauses.</done>
</task>

<task type="auto">
  <name>Task 2: Add EN/ES translations for all 66+ templates</name>
  <files>web/messages/en.json, web/messages/es.json</files>
  <action>
Add a `"templates"` top-level key to both `web/messages/en.json` and `web/messages/es.json` containing translations for every template seeded in Task 1.

**Structure in both files:**
```json
{
  "templates": {
    "lead_followup_email": {
      "name": "Lead Follow-up Email Sequence",
      "description": "Automatically nurture leads with personalized email sequences triggered by website form submissions",
      "impact": "Reduces lead response time from hours to minutes, increasing conversion by up to 35%",
      "metric_label": "Emails sent"
    },
    "ai_chatbot_24_7": {
      "name": "AI Customer Support Chatbot",
      "description": "...",
      "impact": "...",
      "metric_label": "Conversations handled"
    }
    // ... all 66+ templates
  }
}
```

**en.json requirements:**
- `name`: Clear, professional template name in English (Title Case)
- `description`: 1-2 sentences describing what the automation does and the business value
- `impact`: 1 sentence describing typical ROI/time savings (use realistic metrics)
- `metric_label`: Short label for the activity metric (e.g., "Emails sent", "Invoices processed", "Conversations handled", "Reports generated")

**es.json requirements:**
- `name`: Professional Spanish translation (not literal — use natural business Spanish for US/CA Hispanic market)
- `description`: Spanish translation of the description
- `impact`: Spanish translation of the impact text
- `metric_label`: Spanish translation (e.g., "Correos enviados", "Facturas procesadas", "Conversaciones atendidas")

**IMPORTANT:** Read the current en.json and es.json files first. Preserve ALL existing keys. Add the `"templates"` key alongside existing top-level keys (signup, login, resetPassword, landing, etc.). Do NOT overwrite or remove any existing content.

Every i18n key referenced in seed.sql Task 1 MUST have a corresponding entry in both en.json and es.json. Cross-check slug names match exactly.
  </action>
  <verify>
    <automated>cd C:/dev/12ai && node -e "const en=require('./web/messages/en.json'); const es=require('./web/messages/es.json'); const tEn=Object.keys(en.templates||{}); const tEs=Object.keys(es.templates||{}); console.log('EN templates:', tEn.length, 'ES templates:', tEs.length, 'Match:', tEn.length===tEs.length && tEn.length>=66)"</automated>
  </verify>
  <done>Both en.json and es.json have a "templates" key with 66+ entries. Every template key has name, description, impact, and metric_label sub-keys. All existing translation keys preserved. EN and ES key sets match exactly.</done>
</task>

</tasks>

<verification>
1. seed.sql has TRUNCATE CASCADE at top, no ON CONFLICT clauses
2. seed.sql contains 66+ template rows across 8 categories
3. Each template has all new columns populated (setup_price, monthly_price, setup_time_days, industry_tags, connected_apps, typical_impact_text, avg_minutes_per_task, activity_metric_label)
4. All 6 industries appear in industry_tags across the template set
5. en.json and es.json both have 66+ template translation entries with 4 keys each (name, description, impact, metric_label)
6. i18n keys in seed.sql match keys in en.json/es.json exactly
</verification>

<success_criteria>
- 66+ templates seeded across 8 categories and 6 industries
- All prices in integer cents within specified ranges
- EN and ES translations complete and matching for all templates
- Seed is idempotent (TRUNCATE + INSERT, no ON CONFLICT)
</success_criteria>

<output>
After completion, create `.planning/phases/07-schema-seed-data/07-02-SUMMARY.md`
</output>
