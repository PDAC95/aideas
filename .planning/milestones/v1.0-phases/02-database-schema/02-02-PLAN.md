---
phase: 02-database-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - supabase/migrations/20260305000002_automation_business.sql
autonomous: true
requirements:
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - DB-08
  - DB-12

must_haves:
  truths:
    - "automation_templates is readable by all authenticated users (global catalog, not org-scoped)"
    - "automations uses 6-state status: draft, pending_review, active, paused, failed, archived"
    - "automation_requests allows user INSERT with org membership check"
    - "automations, automation_executions, and subscriptions are service_role-only for writes"
    - "All org-scoped tables use EXISTS + organization_members pattern for SELECT"
  artifacts:
    - path: "supabase/migrations/20260305000002_automation_business.sql"
      provides: "Automation templates, automations, executions, requests, subscriptions tables with RLS"
      contains: "CREATE TABLE automation_templates"
  key_links:
    - from: "automations.organization_id"
      to: "organizations.id"
      via: "REFERENCES organizations(id) ON DELETE CASCADE"
      pattern: "REFERENCES organizations"
    - from: "automation_executions RLS"
      to: "automations + organization_members"
      via: "EXISTS check joining through automations to organization_members"
      pattern: "EXISTS.*organization_members.*automations"
---

<objective>
Create the five automation and business tables (automation_templates, automations, automation_executions, automation_requests, subscriptions) with correct RLS policies implementing the hybrid write model.

Purpose: These tables represent the core business domain — what automations exist, their execution history, customer requests, and billing state.
Output: Migration file `20260305000002_automation_business.sql` with all five tables, RLS, indexes, and triggers.
</objective>

<execution_context>
@C:/Users/patri/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/patri/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-database-schema/02-CONTEXT.md
@.planning/phases/02-database-schema/02-RESEARCH.md
@.planning/phases/02-database-schema/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create automation and business tables migration</name>
  <files>supabase/migrations/20260305000002_automation_business.sql</files>
  <action>
Create `supabase/migrations/20260305000002_automation_business.sql` containing:

**1. automation_templates table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `name VARCHAR(255) NOT NULL`
- `slug VARCHAR(100) UNIQUE NOT NULL`
- `description TEXT`
- `category TEXT NOT NULL CHECK (category IN ('customer_service', 'documents', 'marketing', 'sales', 'operations'))`
- `icon VARCHAR(50)`
- `features TEXT[]` (array of feature descriptions)
- `use_cases TEXT[]` (array of use case descriptions)
- `config_schema JSONB DEFAULT '{}'`
- `pricing_tier TEXT DEFAULT 'starter' CHECK (pricing_tier IN ('starter', 'pro', 'business'))`
- `is_active BOOLEAN DEFAULT true`
- `is_featured BOOLEAN DEFAULT false`
- `sort_order INTEGER DEFAULT 0`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- No `deleted_at` — use `is_active` to hide (per research)
- Enable RLS
- SELECT policy: `TO authenticated` with `USING (is_active = true)` — global catalog, not org-scoped (per CONTEXT.md locked decision)
- No INSERT/UPDATE/DELETE policies (service_role only)
- updated_at trigger
- Indexes: `category`, `is_active`, `slug`

**2. automations table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE`
- `template_id UUID REFERENCES automation_templates(id)` — nullable (custom automations)
- `name VARCHAR(255) NOT NULL`
- `description TEXT`
- `status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'pending_review', 'active', 'paused', 'failed', 'archived'))` — 6-state lifecycle per CONTEXT.md
- `config JSONB DEFAULT '{}'`
- `last_run_at TIMESTAMPTZ`
- `next_run_at TIMESTAMPTZ`
- `error_message TEXT`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `deleted_at TIMESTAMPTZ` (soft delete)
- Enable RLS
- SELECT policy: org-scoped via EXISTS on organization_members, `deleted_at IS NULL`, `(SELECT auth.uid())`
- No INSERT/UPDATE/DELETE policies (service_role only)
- updated_at trigger
- Indexes: `organization_id`, `status`, `template_id`, composite `(organization_id, status)`

**3. automation_executions table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `automation_id UUID NOT NULL REFERENCES automations(id) ON DELETE CASCADE`
- `status TEXT NOT NULL CHECK (status IN ('running', 'success', 'error', 'cancelled'))`
- `started_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- `completed_at TIMESTAMPTZ`
- `duration_ms INTEGER`
- `input_data JSONB`
- `output_data JSONB`
- `error_message TEXT`
- `triggered_by TEXT DEFAULT 'schedule' CHECK (triggered_by IN ('schedule', 'manual', 'webhook', 'api'))`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- No `updated_at` or `deleted_at` (execution records are immutable)
- Enable RLS
- SELECT policy: org-scoped via EXISTS joining through `automations` to `organization_members` with `(SELECT auth.uid())`
- No INSERT/UPDATE/DELETE policies (service_role only)
- Indexes: `automation_id`, `status`, `started_at`

**4. automation_requests table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE`
- `template_id UUID REFERENCES automation_templates(id)`
- `user_id UUID NOT NULL REFERENCES profiles(id)`
- `title VARCHAR(255) NOT NULL`
- `description TEXT NOT NULL`
- `urgency TEXT NOT NULL DEFAULT 'normal' CHECK (urgency IN ('low', 'normal', 'urgent'))` — 3-level per CONTEXT.md (not medium/high)
- `status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'in_review', 'approved', 'completed', 'rejected'))`
- `notes TEXT`
- `completed_at TIMESTAMPTZ`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `deleted_at TIMESTAMPTZ` (soft delete)
- Enable RLS
- SELECT policy: org-scoped via EXISTS on organization_members, `deleted_at IS NULL`, `(SELECT auth.uid())`
- INSERT policy: user can INSERT if org membership confirmed via EXISTS on organization_members, `WITH CHECK` verifying `user_id = (SELECT auth.uid())` AND org membership (per CONTEXT.md: interaction tables allow user INSERT)
- No UPDATE/DELETE policies (service_role only for status changes)
- updated_at trigger
- Indexes: `organization_id`, `status`, `user_id`

**5. subscriptions table:**
- `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`
- `organization_id UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE`
- `stripe_customer_id VARCHAR(255)`
- `stripe_subscription_id VARCHAR(255) UNIQUE`
- `plan TEXT NOT NULL DEFAULT 'starter' CHECK (plan IN ('starter', 'pro', 'business'))`
- `status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'past_due', 'cancelled', 'trialing'))`
- `billing_cycle TEXT DEFAULT 'monthly' CHECK (billing_cycle IN ('monthly', 'yearly'))`
- `current_period_start TIMESTAMPTZ`
- `current_period_end TIMESTAMPTZ`
- `cancel_at TIMESTAMPTZ`
- `cancelled_at TIMESTAMPTZ`
- `trial_start TIMESTAMPTZ`
- `trial_end TIMESTAMPTZ`
- `created_at TIMESTAMPTZ DEFAULT NOW()`
- `updated_at TIMESTAMPTZ DEFAULT NOW()`
- `UNIQUE(organization_id)` — one subscription per org
- No `deleted_at` (subscription lifecycle managed by Stripe)
- Enable RLS
- SELECT policy: org-scoped via EXISTS on organization_members, `(SELECT auth.uid())`
- No INSERT/UPDATE/DELETE policies (service_role only — synced from Stripe)
- updated_at trigger
- Indexes: `organization_id`, `stripe_subscription_id`, `status`

**IMPORTANT PATTERNS:**
- All `auth.uid()` MUST be wrapped as `(SELECT auth.uid())`
- All SELECT policies specify `TO authenticated`
- Use CHECK constraints (not Postgres ENUM) for all status/type columns
- Org-scoped SELECT uses `EXISTS (SELECT 1 FROM organization_members WHERE organization_members.organization_id = table.organization_id AND organization_members.user_id = (SELECT auth.uid()))`
- automation_requests INSERT policy uses `WITH CHECK` (not `USING`) to verify org membership AND `user_id = (SELECT auth.uid())`
  </action>
  <verify>
    <automated>bash -c "test -f supabase/migrations/20260305000002_automation_business.sql && grep -q 'CREATE TABLE.*automation_templates' supabase/migrations/20260305000002_automation_business.sql && grep -q 'CREATE TABLE.*automations' supabase/migrations/20260305000002_automation_business.sql && grep -q 'CREATE TABLE.*automation_executions' supabase/migrations/20260305000002_automation_business.sql && grep -q 'CREATE TABLE.*automation_requests' supabase/migrations/20260305000002_automation_business.sql && grep -q 'CREATE TABLE.*subscriptions' supabase/migrations/20260305000002_automation_business.sql && grep -q 'pending_review' supabase/migrations/20260305000002_automation_business.sql && grep -q \"'normal'\" supabase/migrations/20260305000002_automation_business.sql && echo 'PASS: Automation business migration verified' || echo 'FAIL'"</automated>
  </verify>
  <done>
- All 5 tables created with correct columns matching CONTEXT.md specifications
- automation_templates has global read policy (not org-scoped)
- automations uses 6-state status enum via CHECK constraint
- automation_requests uses 3-level urgency (low/normal/urgent) via CHECK constraint
- automation_requests has user INSERT policy with org membership check
- automations, automation_executions, subscriptions are service_role-only for writes
- All org-scoped SELECT policies use EXISTS + organization_members pattern with (SELECT auth.uid())
- Soft delete (deleted_at) on automations and automation_requests
  </done>
</task>

</tasks>

<verification>
1. File exists: `ls supabase/migrations/20260305000002_automation_business.sql`
2. All 5 tables: grep for each CREATE TABLE statement
3. Status values: `grep 'pending_review' supabase/migrations/20260305000002_automation_business.sql` confirms 6-state lifecycle
4. Urgency values: `grep "'normal'" supabase/migrations/20260305000002_automation_business.sql` confirms 3-level urgency (not medium)
5. RLS wrapped: `grep 'auth.uid()' supabase/migrations/20260305000002_automation_business.sql | grep -v 'SELECT auth.uid()'` returns nothing
6. INSERT policy on automation_requests: `grep -A5 'FOR INSERT' supabase/migrations/20260305000002_automation_business.sql` shows WITH CHECK
</verification>

<success_criteria>
- Migration file creates all 5 tables with columns matching CONTEXT.md and RESEARCH.md specs
- Hybrid write model correctly implemented: user INSERT on automation_requests, service_role-only on rest
- All CHECK constraints use correct values (6-state status, 3-level urgency)
- All RLS policies use optimized (SELECT auth.uid()) pattern
- Execution records join through automations for org-scoped access
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-schema/02-02-SUMMARY.md`
</output>
